# PPG Heart Rate Algorithm

This context defines the domain language used when discussing PPG heart-rate estimation across rest, motion, and post-motion periods.

## Language

**动态追踪算法预设**:
v2 心率求解中用于组织不同心率追踪策略、BO 使用范围和固定参数层级的算法方案。
_Avoid_: 算法模式泛称, 参数模板

**v2 运行策略束**:
由动态追踪算法预设和运行配置共同派生的策略集合，用来描述一次 v2 心率求解中的追踪、回切、限幅和保护窗规则。它把策略语义从兼容参数记录中分离出来，供各算法模块一致消费。
_Avoid_: 参数散列表, 单个预设名称

**Lite**:
固定静息、运动和恢复追踪参数的小搜索空间 v2 算法预设，主要用于效率基线和 TraceRescue 候选状态内核。
_Avoid_: 简单版泛称, 无 BO 算法

**TraceRescue**:
样本级运行多个固定候选状态，并用无监督轨迹诊断选择最终状态的 v2 算法预设。
_Avoid_: 在线状态机, 多数投票算法, 场景共享选参主线, 本轮主要比较基线

**机制开发面板**:
本轮允许反复用于恢复、滤波和惩罚机制设计的固定记录集合；它提供受控的机制覆盖，但所有记录均属于开发复用数据，不能支持未见场景或跨个体确认性结论。
_Avoid_: 确认性测试集, 跨场景泛化集, 最终验收集

**跨场景覆盖型滤波组合库**:
在多个机制开发场景上统一设计、数量受限且预先冻结的滤波组合集合；它用于覆盖不同采样率、物理记忆长度和有效步长区域，不等同于按样本或按场景定制参数。
_Avoid_: TraceRescue 状态集, 单样本 BO 空间, 场景专属参数表

**双级串联 HF 滤波基座**:
将两路排序后的 HF 参考依次执行自适应更新的现有滤波结构，是本轮低锁上跳修复与共享参数实验的冻结算法基座。它保留已知频谱风险并由正式安全门筛选，不因 rank-1 已有开发证据而在本轮直接换基座。
_Avoid_: rank-1 滤波, 双路联合一次更新, 已证明全记录频谱安全

**dual_cascade_two_hf_v1**:
双级串联基座的可执行身份：保持当前生产与修复证据使用的 `reference_groups_order=("HF",)`、`adaptive_reference_stage_limit=None`，同时在求解前后强制验证实际恰好执行两级 HF 自适应更新；实际级数不是二时失败关闭。它与显式 `stage_limit=1` 的 rank-1 身份、缓存和结论完全隔离。
_Avoid_: 显式 stage_limit=2 新身份, None 表示级数不限不审计, rank-1 缓存复用, HF 参考数量变化静默放行

**rank-1 滤波候选**:
只保留排序后第一 HF 参考级自适应更新的待验证滤波方案；现有开发证据只证明其频谱复现与安全门表现，尚未证明 MAE、长错误段或恢复收益。本轮不把它作为算法基座，后续另行设计收益验证。
_Avoid_: 本轮冻结滤波基座, 已证明综合性能更优, 双级串联默认行为

**场景共享滤波档位**:
从跨场景覆盖型滤波组合库中，仅依据同一场景训练记录选出的一个固定组合，并原样应用于该折的留出记录。
_Avoid_: 测试记录择优, 样本级动态切换, 全场景统一档位

**组合库样本内上限**:
对单条记录枚举跨场景覆盖型滤波组合库后，使用该记录结果选出的最佳档位所形成的诊断上限；它用于判断组合库覆盖是否足够，不是可部署方法或正式基线。
_Avoid_: 场景共享结果, 独立 BO 基线, 可部署选择器

**场景内选择器重放审计**:
恢复机制、惩罚策略和滤波组合库已经使用全部开发记录形成后，仅限制档位选择器在每折读取两条训练记录并向第三条记录重放的开发内审计。它只能检查选择器的折内读取隔离，不能证明整套算法未使用目标记录或已经实现迁移。
_Avoid_: 场景内迁移验证, 算法级留出评估, 未见记录泛化

**历史独立 BO 工程精度锚点**:
每条记录使用自身参考心率、绑定旧算法源码完成的独立 BO Lite 结果。它只保留为历史精度参照和新实验的候选调度线索，不能充当优化后算法的正式基线或正式安全候选证据。
_Avoid_: 当前源码基线, 新算法正式结果, 可直接复用的求解缓存

**当前源码独立 BO 工程精度锚点**:
每条记录使用自身参考心率、绑定同一冻结优化后源码，由 Lite 批量全流程在默认六维空间中以三次独立 repeat、每次 50 trials 重建的结果。不同 repeat 允许搜索到重复参数组合；它表示当前算法的样本内拟合能力上限，并交付逐样本汇总与心率曲线，不承担可迁移性或机制因果归因。
_Avoid_: 历史归档锚点, 共享参数结果, 机制非退化证据, 150 个全局不重复物理组合

**Lite 上限与搜索稳定性双口径**:
每条记录以三个 repeat 共 150 个逻辑 trial 中的全局最佳合格结果作为正式工程上限，同时报告 seed 42/43/44 各自最佳 MAE 与参数、repeat 中位数和极差、不同坐标数、重复数、solver 缓存命中、最佳值首次出现位置与运行时间。新旧防退化比较使用各自全局最佳，触发门槛后以旧最佳参数在新源码下固定重放区分机制退化与搜索路径差异。
_Avoid_: 三次均值充当性能上限, 只展示幸运 repeat, 重复 trial 冒充不同候选, 搜索波动解释为机制退化

**Lite-150 与已知最佳当前源码双锚点**:
未触发固定重放时，两者均为常规 Lite `3 × 50` 全局最佳；若代理审核触发旧最佳参数在新源码下固定重放，则 Lite-150 继续严格记录 150 个逻辑 trial 的结果，已知最佳当前源码锚点取 Lite-150 与固定重放中的较优结果。共享面板安全门使用后者，但报告不得把额外重放伪装进 Lite-150 搜索。
_Avoid_: 151 次冒充 150 次, BO 漏搜放宽共享门, 固定重放覆盖搜索历史, 双锚点不标来源

**Lite 基线更新面板**:
历史 `20260724_001816` Lite 批量全流程中的完整 24 条 LYX 记录，用于逐条重建当前源码独立 BO 工程精度锚点并审核跨场景防退化。它比后续四场景 12 条共享参数主面板更宽，但仍是开发复用数据，不构成未见数据验证。
_Avoid_: 共享参数主面板, 确认性泛化集, 只含 12 条记录的机制面板

**Lite 基线更新代理审核门**:
对 24 条 Lite 基线更新记录逐条比较新旧全段与运动段误差；任一记录退化超过 0.5 BPM 或长错误段变差即由执行代理补做旧最佳参数在新源码下的固定重放并审阅曲线，退化超过 2 BPM、新增严重长错误段或重放确认机制退化则停止。代理按预注册标准写入放行或停止回执，不等待夜间人工确认；面板平均改善不能抵消单记录触发条件。
_Avoid_: 等待在线人工批准, 平均无退化门, 无审核自动放行, 用 kaihe3 收益抵消其他记录退化

**不重复物理组合上限诊断**:
在专项四维物理空间中按全局不同参数坐标计量预算，并用确定性补齐形成指定数量唯一组合的重型逐记录诊断。它属于上一轮恢复与共享参数路线，不是本轮用于更新 Lite 样本性能上限的批量全流程独立 BO。
_Avoid_: Lite 3×50 trials, 允许重复的独立 repeat, 常规批量全流程

**双空间实验分工**:
默认六维 Lite 空间只用于更新当前源码的逐样本独立 BO 性能上限；四维物理空间用于研究固定参数组合能否形成稳定的场景共享平台。两者使用同一双级串联滤波与低锁上跳修复；完全相同求解键允许复用底层数值，但候选身份、评价回执、参数交集和搜索结论必须分开。
_Avoid_: 用六维 Lite 直接做共享选参, 用四维专项结果冒充 Lite 基线, 混合空间候选交集, 因跨阶段缓存命中合并证据身份

**两层内容寻址缓存**:
solver 数值缓存按修复后源码树、原始数据哈希、完整算法配置、双级联结构和机制开关寻址，不含实验阶段、fold 或记录别名；评价缓存另按 solver 结果、参考心率哈希和指标/安全门合同寻址。Lite 重复 trial、Lite 与物理面板重叠坐标以及 fold 重复请求均可复用 solver 数值，但必须保留各自逻辑身份和评价回执；旧源码结果只能复用坐标与调度线索。
_Avoid_: 以阶段名阻断数值复用, 参考变化仍复用旧指标, 缓存命中省略逻辑 trial, 旧源码数值混入新结论

**25 Hz 扩展物理组合面板**:
固定 25 Hz 处理档位，以 `40/80/120/160/200/320/480/640/800 ms` 九档物理记忆、五档 `mu` 和四档运动频率排除半宽形成的 180 个确定性双级串联组合。九档记忆对应 `1/2/3/4/5/8/12/16/20 taps`；沿用 `physical_v1` 固定项 `smooth_win_len=5`、`time_bias=5.0 s`、`lms_mu_min=1e-6` 和全记录分析，不使用 BO 建议顺序或全局不重复补齐。
_Avoid_: 原 100 组合短记忆面板, 25/50/100 Hz 全空间, 六维 Lite 搜索, 150 个 BO 候选

**场景公共可行性短路筛查**:
按预先冻结的记录顺序，将 25 Hz 扩展物理组合面板先运行于场景第一条记录，之后只把累计通过逐记录安全门的组合传给下一条，精确求得三记录公共安全交集。它用于判断共享空间是否存在共同可行区域并节省计算，不是两条选择、一条揭示的三折选择器证据。
_Avoid_: BO 候选淘汰, 三折选择器审计, 用公共交集冒充留出泛化, 失败组合继续补算

**场景级共享失败归因**:
对每个公共安全交集为空的场景分别识别其主要失败机制，允许不同场景得到不同主因；只有逐场景证据完成后，才能进一步归纳跨场景共同的上层结构。
_Avoid_: 预设单一共同根因, 把空交集直接等同于参数空间无解, 用场景平均掩盖不同失败机制

**共享失败矩形响应面**:
在源码、候选面板和评价合同冻结时，为空交集场景的每条记录补齐同一完整候选集的指标与安全判定，用于区分记录内不可行和跨记录安全集合错位。
_Avoid_: 公共可行性短路结果, 新一轮 BO, 扩大候选空间, 重算已有可信缓存

**平台稳定性与留出预测稳定性**:
平台稳定性描述相邻物理参数扰动下是否仍安全、性能是否平坦；留出预测稳定性描述只读取训练记录选参后，冻结参数在留出记录上的心率结果是否通过完整评价合同。历史证据表明平台稳定性既不是留出预测稳定性的充分条件，也不是必要条件，只能作为选择器解释或消融变量。
_Avoid_: 以平台存在证明留出准确, 以无平台否定跨记录共享, 以坐标一致证明预测稳定, 合并成单一通过标志

**三折留出预测通过**:
三个 fold 各自只读取两条训练记录选择并冻结一个坐标后，三个留出记录的心率结果均通过冻结的整体与局部评价合同。三个 fold 是否选中相同坐标、相邻坐标或同一连通区只作选择行为诊断，不参与主通过裁决。
_Avoid_: 三折坐标一致, 稳定参数平台, 留出平均通过, 用场景平均补偿失败折

**三折后全三条开发拟合**:
达到三折留出预测通过后，才允许使用冻结的选择规则读取三条开发记录并选出一个最终场景档位。该步骤只服务未来固定应用，不因三折坐标是否一致而改变通过结论，也不增加三折或泛化证据。
_Avoid_: 全三条拟合冒充留出验证, 根据三折结果改选择规则, 要求三折同坐标才产出档位, 无审计场景参数晋级

**三折完整候选池与指标隔离**:
每个 fold 的概念候选池始终是当前实验合同冻结的完整物理组合；当前四维主线为 `3×5×5×4=300` 个坐标，旧固定 25 Hz 扩展面板则为 180 个，二者不得混用。候选资格只读取两条训练记录；已读取三条记录形成的公共安全交集不能替 fold 预先筛候选，第三条记录的指标必须在参数与排序回执冻结后才能揭示给选择器。
_Avoid_: 公共交集作为 fold 候选池, 留出指标预淘汰, 每个 fold 重算同一求解, 缓存存在即允许选参读取

**历史近优平台优先选择器**:
旧三折研究中先按训练硬门和最坏 MAE形成近优池，再优先选择安全邻居较多、参数悬崖较少坐标的开发选择规则。历史结果已证明平台分数不能单独预测留出准确，因此它只作为当前选择器原型的对照和消融，不再是默认规则。
_Avoid_: 当前冻结选择器, 平台稳定即跨记录稳定, 留出邻域支持, 结果后调整近优容差

**训练共同准确集**:
在一个三折 fold 中同时通过两条训练记录 `engineering_pass_v2` 的完整物理坐标集合，是选择器唯一允许排序和冻结参数的候选域。
_Avoid_: 三记录公共交集, 留出安全集, BO 近优 trial

**三记录共同准确集**:
训练共同准确集中进一步在隐藏留出记录也通过 `engineering_pass_v2` 的坐标子集。它只用于 prototype 的开发标签和冻结后的揭盲验收，不得在正式选择时作为候选过滤条件。
_Avoid_: 选择器可见候选池, 训练安全集, 可部署 oracle

**三折选择器证据原型**:
只读取冻结的完整响应面，比较有限且可解释的训练侧选择原则能否提高命中三记录共同准确集概率的 throwaway 开发分析。它不运行 solver 或 BO，不产生正式选择器结论，也不把平台稳定性预设为主导证据。
_Avoid_: 正式三折执行器, 自动规则搜索, 生产参数选择器

**训练侧排序辨识失败**:
每个留一折的三记录共同准确集均非空，但冻结选择规则仍无法只凭两条训练记录稳定地把其中坐标排到首位。它说明共享参数在候选空间中可达、当前排序证据不足，不等于共享参数不存在、BO 漏搜或算法已经失去跨穿戴泛化潜力。
_Avoid_: 参数空间不可达, 算法泛化失败, BO搜索失败, 不存在任何选择器

**失败前沿可辨识性诊断**:
在选择规则揭盲失败后，冻结各既有规则的近前沿候选，先补齐已真实运行但未持久化的 direct 时序与机制报告并核对冻结性能身份，再比较失败坐标和相邻安全坐标的时序误差及训练侧可观测求解证据。它用于判断是否存在值得预注册的新证据族，不直接产出新选择规则，也不得把揭盲后的 top-k 命中升级为选择策略。
_Avoid_: 第五条选择规则, top-k投票选择器, 按留出结果调阈值, 自动扩大机制矩阵

**训练侧信号未辨识**:
失败前沿的 direct 证据与冻结性能身份均有效，但预注册的轨迹/频谱一致性、状态持续/恢复能力和双训练记录稳健性三类证据，没有形成一个能以同一方向区分 Run1 与 Run3 安全/失败坐标的训练侧信号。该裁决只否定当前有限证据族继续直接生成选择规则，不证明所有可能选择器不存在；在获得新证据前，暂停第五条规则、权重或阈值工程。
_Avoid_: 不存在任何选择器, 4D空间不可达, BO漏搜, 允许开放特征挖掘

**参数条件机制脆弱性**:
冻结算法只在部分训练共同准确、部署时可能被选择的参数坐标下，由既有机制处置放大为灾难性错误轨迹，而配对参数坐标不产生同一后果。它同时涉及参数选择风险和运行时机制稳健性，不等于参数本身错误、所有坐标共有的算法缺陷或单样本专用 bug。
_Avoid_: 全局机制缺陷, 纯选参失败, 必须使用失败参数, 单坐标即授权修机制

**参数—机制责任分界门**:
在生产机制修改前，先判断失败区域能否由只读训练记录的冻结资格规则可靠排除，以及既有机制 seam 是否在多个部署可达参数上产生可重复因果伤害。只有训练侧无法可靠规避、跨参数因果成立且安全参数不退化时，才把后续最小修补归入机制合同责任。
_Avoid_: 看见安全坐标即只修选择器, 救回两个红点即改生产, 用留出标签制定参数黑名单, 参数与机制二选一

**统一运行时错误轨迹恢复**:
当训练记录不足以预判某一共享坐标在新穿戴记录上的错误轨迹风险时，改由冻结心率算法在当前记录运行过程中，依据无需 HR_ref 即可获得的求解状态和频谱证据，识别失去可信支持的跟踪轨迹并作有界恢复。候选机制必须用同一接口和规则覆盖 Jianpan、Kaihe、Run、Quanji，不读取场景或记录身份；Run1 尾端未恢复与 Run3 中段长错误只能作为发现和开发标签，不能变成两个场景专用补丁。
_Avoid_: Run专用恢复, HR_ref触发, 按G5或G2/G3写死分支, 选择器重排, 在线BO

**既有机制修补优先**:
错误轨迹已经落入现有运动段高锁逃逸、低锁重捕获、谱峰惩罚/历史保护、rise 谱系、运动后 PM-CHR 或 Final 后处理的职责范围时，先核对该机制是否未触发、证据被上游遮蔽、动作被下游 writer 抵消、状态未闭合或与另一机制冲突。只有现有职责边界确实无法承载所需观测与处置时，才允许提议新机制；修补必须保留单一所有者和单一 Final 写入边界。
_Avoid_: 同义恢复器, 并列Final控制器, 为单个评价门新增机制, 未做消融先加状态机

**在线状态传播反事实**:
在不改变既有触发观测、目标和内部动作的前提下，把某机制的内部恢复结果从授权时点提交给既有下游链，并让后续窗口从新的状态继续运行的因果实验。它用于判断恢复动作是否被下游动态抵消；事后只改写若干输出点的 shadow 曲线不属于在线状态传播证据。
_Avoid_: 离线曲线拼接, 新Final写入者, 生产修复, 最终PASS即必要原因

**候选删除反事实**:
保持运动惩罚的中心、权重、置信度和观测完全不变，只取消候选从可选择集合中的删除，并逐窗记录原本会删除的候选及其上下文的因果实验。它用于区分惩罚证据本身与候选可见性处置，不等于关闭运动惩罚。
_Avoid_: penalty-off, 改惩罚权重, 不带窗口映射的would-remove列表, 生产默认

**非破坏性运动惩罚**:
在运动伪影频率可能与真实运动心率重叠时，仍依据当前信号证据对候选降权，但不永久丢弃惩罚带内峰的证据。保留证据不等于立即赋予主选择资格；任何重新进入主选择器的条件都不得读取场景、记录或 HR_ref 身份。
_Avoid_: penalty-off, 加权全可见, Run3专用修复, 运动主频必然是伪影, 带内候选无条件删除, HR_ref门控

**加权全可见惩罚**:
保留既有运动惩罚权重，但让所有惩罚带内峰立即进入主选择器的候选集合。该处置会在 Kaihe 开发记录中重新放出占优的运动主频或倍频，因此不再作为非破坏性运动惩罚的目标语义。
_Avoid_: 非破坏性运动惩罚, 条件释放, 影子候选

**影子候选保留**:
惩罚带内峰继续保留在可审计的证据池中，但默认不取得主选择资格；只有满足统一、无标签且来自当前信号的条件时，才可被释放给既有选择器。它不是新的 Final writer，也不拥有心率轨迹。
_Avoid_: 全部峰立即可选, 场景白名单, 参数黑名单, HR_ref释放门

**受压保护目标**:
上一轨迹已由历史保护认可、但本窗因 challenger 成立而失去主选择资格的惩罚带内候选。它可以被短暂保留为影子候选，但不能因此直接取得 Final 写入权；普通未受保护的运动主频或倍频不属于该目标。
_Avoid_: 任意would-remove峰, challenger本身, 普通运动峰, Run3专用候选

**受压保护目标影子所有权**:
历史保护在 challenger 压制发生时，对原受保护目标取得的有界证据所有权；它只允许跨少量运动窗口匹配并保留该目标，不覆盖当窗 challenger，也不拥有 Final。目标缺失、漂移失配、自然离开惩罚带、到期、运动结束或 EOF 都终止所有权。
_Avoid_: 永久候选锁, Final所有权, 全惩罚带所有权, 无界跨窗冻结

**受压保护目标同窗证据可见性**:
历史保护目标被 challenger 压制时，该目标只在同一窗口保留于下游候选证据池；它不替换 challenger、不强制选中、不写 Final，也不跨窗持有目标。
_Avoid_: 全惩罚带可见, 同窗强制恢复, 延迟影子释放, 场景特判

**受压保护目标连续可见性**:
受压保护目标在 challenger 压制后，于有界所有权存续期间持续进入后续运动窗口的加权候选集合；是否采用仍由既有选择器决定，不强制写入 Final。
_Avoid_: 单次影子释放, 同窗证据可见性, 强制恢复, 全惩罚带可见

**运行时机制作用起点**:
候选机制首次改变当窗因果求解路径的窗口；只建立 owner 或只改变审计元数据不构成作用起点。
_Avoid_: acquire窗, 报告Final首变窗, 机制声明窗

**离线报告可见起点**:
离线平滑和后处理后的 Final 首次显示候选差异的窗口。居中平滑可使它早于运行时机制作用起点，但这不表示运行时状态提前消费未来信息。
_Avoid_: 运行时作用起点, 状态转移窗, 提前触发窗

**活跃所有权审计差异**:
候选与冻结对照的心率结果、指标和评价门一致，但活跃机制的生命周期或所有权证据不同。它需要同算法身份复算来关闭确定性，不等同于性能退化或机制失败。
_Avoid_: 退化单元, 数值风险, 无效实验, trace噪声

**开发揭盲数据**:
留出记录的性能、失败门或曲线一旦被用于解释失败、构造特征或修改选择规则，该记录即属于选择器开发数据。之后在同一批记录上重放留一折只能证明开发回代与读取隔离，不能再次充当新规则的独立泛化验收。
_Avoid_: 揭盲后独立测试集, 旧三折再次确认, 12折独立重复, 开发回代泛化率

**跨场景统一选择规则**:
同一组候选资格、训练侧特征、排序键与并列规则机械应用于 Jianpan、Kaihe、Run 和 Quanji，不读取场景名称切换公式。某一场景的失败可用于发现通用风险证据，但据此形成的规则必须重新检查四场景全部十二个留一折，不能用其他场景原有通过结果代替非回归复算。
_Avoid_: Run专用分支, 按场景调阈值, 场景名称特征, 只复算失败折

**选择器可用运行时证据**:
在未知留出记录结果时，可由冻结算法处理两条训练记录自然产生，并能在未来同一选参流程中以相同方式计算的观测或聚合。它不得依赖留出 HR_ref、留出误差标签、场景名称或仅为实验审计构造的所有权账本。
_Avoid_: 留出解释标签, 机制所有权审计字段, 场景特征, HR_ref派生风险标签

**完整响应面离线三折**:
在选择原则冻结后，使用当前四维空间全部 300 个坐标的既有响应面正式复算每个 fold 的候选资格、排序、冻结和留出结果。它隔离并验证选择规则，不包含 BO 搜索误差；只有该阶段通过后，才允许另行研究有限预算搜索能否可靠复现其选择能力。
_Avoid_: BO 三折, 新记录在线优化, 只复算 top-1, 用留出结果修订规则

**开发期有限预算搜索**:
在完整响应面离线三折已经证明选择规则有效后，用于减少训练记录参数评估次数的候选发现过程。它只能逼近冻结选择规则在完整候选池上的结果，不参与未来新穿戴记录的心率求解，也不得改变评价合同或选择原则。
_Avoid_: 新记录 BO, 选择规则学习器, 完整响应面三折的替代证据

**已有风险候选响应面分析**:
在快速收尾证明候选算法的数值结果与确认决策没有变化后，复用该候选原有完整矩阵开展的只读选择器分析。它必须绑定原矩阵的冻结源码与证据身份，并显式保留已知风险；不得把旧矩阵表述成当前工作树的新一次完整运行。
_Avoid_: 当前源码响应面, 风险已消除矩阵, 证据路径修复等于算法重新验收

**按场景留一规则审计**:
每次隐藏一个完整运动场景，只用其余三个场景的开发三折结果从预注册选择原则中冻结一条规则，再在隐藏场景检验三个 fold 的留出预测。它用于检查规则是否依赖单一场景，不替代新增穿戴记录上的正式泛化验证。
_Avoid_: 参数三折, 跨个体验证, 用隐藏场景回调规则

**选择器连续安全裕量**:
训练共同准确集中，候选相对各适用连续评价门失败边界的 fold 内百分位，并以其中最差百分位表示候选的最弱安全余量。布尔结构门只作资格过滤，不参与跨单位求和或权重学习。
_Avoid_: 原始单位直接相加, 学习门权重, 留出裕量评分

**三折留出曲线审阅**:
对冻结选择结果使用项目批量全流程的统一心率绘图风格生成留出记录曲线；展示 HF 自适应滤波结果时同步绘制 ACC 对比参考信号。曲线用于解释整体与局部误差，不进入选择器打分或参数冻结。
_Avoid_: prototype 专属绘图风格, 只画 HF 不画 ACC, 目视曲线参与选参

**场景独立失败与总体全分母**:
公共交集为空、训练对无安全候选或留出失败只停止并记录对应场景，其他场景继续形成独立诊断；源码、输入、指标、runner 或缓存完整性异常以及系统性算法错误才停止整体实验。最终四场景全部保留在分母中，任一场景失败即不得发布“四场景稳定共享参数通过”，成功场景也不得通过平均值补偿失败场景。
_Avoid_: 首个场景失败即终止全实验, 只报告成功场景, 场景平均补偿, 局部通过冒充总体通过

**十二小时墙钟硬上限**:
本轮最多连续运行 12 小时；达到上限后在记录或场景事务边界写入可恢复 checkpoint/completion 并正常暂停，不删除记录、缩减 Lite trial、缩小 180 组合或自动扩展新滤波/机制。账本分别记录 3,600 个 Lite 逻辑 trial、最多 24 次退化重放、最多 2,160 个物理坐标请求、唯一新 solver 数、缓存命中与剩余预算。
_Avoid_: 两天无界运行, 为赶时删矩阵, 超时身份半写入, 缓存命中冒充逻辑请求减少

**双审核包交付**:
Lite 包含 24 条新旧逐记录与场景汇总、repeat/坐标/缓存/运行时统计、逐记录叠加曲线、触发重放曲线和总览图；共享包包含场景存活漏斗、公共安全连通域、分层平台、12 fold 回执、留出曲线、三折坐标一致性和最终场景参数。统计同时保存 CSV/JSON，审阅图默认 600 dpi PNG；不为全部物理候选批量生成无审核价值的曲线。
_Avoid_: 只交平均表, 数千候选全量绘图, 无机器可读回执, 只展示成功曲线

**分层平台评价**:
同一批公共安全结果零运行派生三层局部鲁棒性证据：安全平台要求一个相邻 `2×2` 的四点在三条记录上均通过八项硬门；强平坦平台进一步要求每条记录内四点 MAE 极差均不超过 1 BPM；宽松敏感性层报告不超过 2 BPM 的平台数量但不替代主标准。报告同时给出平台数量、参数覆盖和最大连通区域，不只给布尔结果。
_Avoid_: 只报 1 BPM 单阈值, 1.01 BPM 等同完全无平台, 用 2 BPM 敏感性替代主标准, 平台存在性单一总分

**场景短路记录顺序**:
场景公共可行性筛查预先冻结为：写字 `xiezi2 → xiezi4 → xiezi3`，键盘 `jianpan1 → jianpan2 → jianpan3`，跑步 `run3 → run1 → run2`，开合跳 `kaihe3 → kaihe1_0617 → kaihe1_0613`。顺序仅利用旧结果安排预计困难记录优先以节省求解，不改变公共交集定义，也不得在看到本轮结果后重排。
_Avoid_: 本轮难度排名, 结果后换序, 用旧失败淘汰新源码样本, 省略第三条记录

**新版共享面板安全门结构**:
本轮共享参数面板冻结为八项逐记录硬门加后置场景约束。逐记录硬门覆盖双级联滤波频谱安全、相对当前源码 Lite 独立 BO 的 L10/L20/MAE 差距、右删失恢复与真实上升保护，以及相对另行冻结的同源码当前控制的 L10/MAE 防退化；场景训练对约束只在后续三折选择器审计中应用。任一逐记录硬门失败即判该组合在该记录不安全，无安全候选时不得选择最小坏值。
_Avoid_: 九项全部塞入首轮短路, 旧源码 Lite 锚点, 平均收益抵消硬失败, 最小坏值兜底

**版本化验收合同**:
带有唯一身份、适用范围和证据等级的冻结评价规则集合；旧实验继续由其原合同解释，规则变化必须产生新版本，不能追溯改写旧结论。
_Avoid_: 永久全局门槛, 当前最新标准覆盖全部历史, 从实验结果反推合同

**合同身份**:
由合同 ID、注册表内容哈希、评价器内容哈希、指标 schema 版本、基线身份和评价时间偏置共同构成的可复核身份；proposal 与完成回执必须绑定同一身份。
_Avoid_: 只写合同名称, 凭记忆复述门槛, 只记录代码提交

**开发验收**:
在已声明开发数据、算法身份和评价口径内判断候选是否值得继续研究的证据等级；它不等同于生产放行、未见记录确认或最终阈值采用。
_Avoid_: 最终生产标准, 泛化通过, 临时诊断结果

**有界开发候选**:
在冻结开发数据、因果面板、非回归面板和确定性审计内通过，但仍默认关闭且尚未通过新穿戴记录验证的算法身份。它可以进入扩大验证，不等于生产默认或泛化结论。
_Avoid_: 生产方案, 四场景泛化通过, 独立验证完成, 默认启用机制

**已知开发缺陷**:
在开发数据上可复现且尚未解决、经范围与收益权衡后暂不继续针对同一数据优化的失败。它必须登记影响范围、证据身份和重开条件，不表示对应评价门已经通过或生产风险已被接受。
_Avoid_: 可忽略误差, 已修复问题, 生产已接受风险, 无需再验证

**工程可接受**:
候选在每条适用记录上满足合同规定的有限 MAE 退化和全部开发硬门的最低通过层；场景均值不能补偿单记录失败。
_Avoid_: 基本持平, 只看平均 MAE, 最小坏值兜底

**强持平**:
工程可接受之上的严格层，要求候选相对公平独立基线同时满足更紧的逐记录绝对、相对和场景聚合 MAE 约束；它仍是开发期结论。
_Avoid_: 工程可接受, 完全等价, 生产精度认证

**L10/L20**:
在完整评价时间轴上，绝对误差分别达到 10 BPM 或 20 BPM 的最长连续时段，单位为秒；连续性由窗口索引和中心时间共同确认。
_Avoid_: E10/E20 原始计数, 压缩数组中的相邻元素数, 窗口个数不标单位

**右删失恢复**:
严重误差 episode 已开始，但直到记录末尾仍未满足合同恢复条件的状态；它表示恢复结果不可观测完成，而不是普通的长错误计数。
_Avoid_: 任意末尾误差, baseline 相对计数, 压缩运动序列的 0/1 字段

**真实上升 episode**:
仅由参考轨迹在完整时间轴上定义、互不重叠的真实心率上升事件，用于离线评价候选的上升低估；它不能由候选输出或在线算法状态定义。
_Avoid_: 候选触发事件, 重叠子数组, 在线参考心率门控

**评价时间偏置**:
评价时将算法 Final 与参考心率对齐的统一时间偏移；它只改变指标统计，不改变算法轨迹，候选与基线必须使用同一取值。
_Avoid_: 算法时延机制, 逐记录择优偏置, 候选与基线不同偏置

**200 ms 双级统一当前控制**:
本轮共享面板用于逐记录防退化比较的固定标准尺：`fs_target=25 Hz`、物理记忆 200 ms（5 taps）、`mu=0.010`、运动频率排除半宽 6 BPM、平滑长度 5、`time_bias=5 s`，使用双级联 HF 滤波和修复后的低锁上跳重捕获。该坐标属于 180 组合面板且已有修复配对证据，但不预设为最佳或共享参数。
_Avoid_: Lite 独立 BO 上限, 200 ms 最优参数, rank-1 控制, kaihe3 专用参数

**跨记录机制非退化配对审计**:
在冻结参数坐标、输入和评价窗口下，对开发面板逐记录比较机制修复前后的结果，先判断修复是否在目标样本之外造成系统性退化。准入由逐记录硬门裁决，目标样本的巨大收益或面板平均改善不得抵消任一记录的明显退化；它不替代当前源码独立 BO 工程精度锚点或共享参数审计。
_Avoid_: 完整独立 BO, 共享参数选择, 未见数据泛化, 只比较各自最优点, 用平均收益掩盖单记录退化

**同角色当前机制对照**:
当前恢复与惩罚机制使用和新方案完全相同的代码身份、滤波组合库、折内数据角色、选择规则和评价合同形成的配对对照。它用于隔离机制变化的开发内收益，不因数据角色公平而自动成为未见数据证据。
_Avoid_: 历史归档曲线直接对比, 独立 BO 拟合上限, 确认性泛化基线

**恢复—滤波交互审计**:
恢复机制初选后固定暂定胜者与唯一备选，在滤波组合库及惩罚策略冻结后，用同惩罚、同档位、同记录的完整配对矩阵判断是否回滚。最多允许一次切换到预先登记备选，禁止循环联合调参或回滚后再次选择惩罚。
_Avoid_: 恢复和滤波联合 BO, 惩罚前提前回滚, 无限往返调参, 只看最终 MAE

**求解尝试注册表**:
实验设计生效后，对开发记录产生的全部探索、诊断和正式 solver/config/record 身份及运行状态的统一账本；未登记的新求解结果不得提名正式候选或参数。
_Avoid_: 只登记最终胜者, 免费探索预算, 用 diagnostic 标签绕过候选上限

**独立 BO 人工审核门**:
只有预声明证据表明现有独立 BO Lite 已无法公平解释新机制表现时，才提交完整新求解器独立 BO 的人工审核节点；未经明确批准不得自动运行。
审批必须绑定求解器、搜索空间、指标、种子和唯一预算哈希；任一身份不匹配都失败关闭。
_Avoid_: 流程默认步骤, 自动补跑 BO, 口头授权, 用计算预算替代审核证据

**运动段**:
PPG 信号受到显著运动伪影影响、算法需要借助运动参考信号或运动感知策略估计心率的时间段。
_Avoid_: motion 区间, 运动窗口

**运动段高频锁定**:
运动段中真实心率峰仍存在于候选谱峰里，但谱峰追踪状态被更高频的运动伪峰、运动谐波或受保护的错误轨迹持续吸引，导致心率估计偏高。
_Avoid_: 高频漂移泛称, 运动后高漂移

**高频锁定逃逸**:
运动段高频锁定被连续证据确认后，算法放宽普通运动段的生理限幅约束，使用独立且更激进的下降策略逐步回到稳定的较低候选心率轨迹。
_Avoid_: 直接切低, 普通运动段限幅

**窗口失效原因分类**:
窗口诊断中用于描述心率估计失效主因的标签体系。每个窗口应有唯一主因，并可附加多个辅助标签，以便跨样本统计典型失效模式。
_Avoid_: 临时 debug 字段, 只看误差大小

**窗口失败主因**:
窗口失效原因分类中的唯一主标签，用于说明该窗口最终心率估计偏离参考心率的最主要机制。它与辅助标签共同使用，避免同一窗口在主统计中被重复计数。
_Avoid_: 多主因并列, 只按误差大小排序

**窗口 trace**:
每个心率估计窗口随结果保存的决策事实记录，描述候选峰、追踪范围、惩罚/保护状态和输出路径来源。它是窗口诊断决策语义的权威来源。
_Avoid_: 本地重放结论, 临时 debug summary

**真实峰可见性**:
自适应滤波后、进入后处理前的窗口频谱中，参考心率附近是否存在可用候选峰及其排名、相对幅值和与伪峰的分离程度。它只评价频谱证据本身，不包含追踪限幅、重捕获或最终输出是否采用该峰。
_Avoid_: 频谱干净度泛称, 最终误差反推峰存在

**真实峰可达性**:
真实心率峰在当前窗口是否能被算法状态实际采用，包括是否落入搜索范围、是否被历史心率、保护带、重捕获状态或高频锁定逃逸状态排除。它用于区分“峰不存在”和“峰存在但链路状态不可达”的失败模式。
_Avoid_: 谱峰命中率泛称, 只看候选峰排名

**频谱证据门槛**:
HF 自适应消噪候选必须先以尺度不变指标证明真实心率峰得到保留或增强、运动伪峰得到抑制且残余伪影关联降低，才有资格进入最终心率性能比较。未通过该门槛的候选不能仅凭最终 MAE 改善晋级。
_Avoid_: 只凭 Final HR 误差判断滤波质量, 整体压低频谱, 单一峰可见率

**下游任务门槛**:
通过频谱证据门槛的候选还必须在固定心率追踪口径下改善运动段误差、命中率和失锁风险，才能视为完整算法收益；只有频谱改善而无下游改善时，归入滤波器—追踪器接口诊断，不视为本轮算法成功。
_Avoid_: 只看平均 AAE, 用追踪后处理替代频谱改善, 只看代表样本

**机制发现阶段**:
使用单样本、单场景或单个体数据快速验证双侧 HF 联合参考如何改善运动伪影消除与真实心率峰暴露的探索阶段。
_Avoid_: 跨人泛化定稿, 最终论文效应量, 锁定确认实验

**分阶段机制 DOE**:
依次研究阶数与固定步长、稳定性机制、数据驱动变步长和冻结扩展效果的实验组织方式，每阶段只将通过两级门槛的稳健候选传给下一阶段。
_Avoid_: 一次性全参数 BO, 全组合无筛选穷举, 只保留最优试验

**稳健参数平台**:
在相邻参数组合及多个样本上持续通过频谱和下游任务门槛的参数区域，优先级高于单个样本上的最低误差点。
_Avoid_: 单点最优参数, BO 最佳 trial, 样本特化参数

**不重复候选预算**:
以实际获得结果的全局不同参数组合数量计量 BO 或分阶段搜索覆盖；独立随机重复之间的重合候选只计一次全局覆盖，但仍保留为重复稳定性证据。报告必须区分建议总数、每次重复内部不重复数、跨重复重合数、全局不重复数、缓存命中数和空间覆盖率。
_Avoid_: trial 行数预算, 重复候选重复求解, 只报 Optuna trial 数

**独立 seed lane**:
一个随机种子只根据自身候选与目标历史继续建议的完整优化重复；不同 lane 可以提出相同候选，该重合用于评价搜索稳定性，不能因共享缓存而改变任一 lane 的建议轨迹。
_Avoid_: 全局排他 seed, 共享历史重复, 把缓存 lane 当独立重复

**自适应非伤害门槛**:
共享参数候选在每条训练记录上都必须满足的资格条件：在同一组有效严格运动窗口上，`LMS+H Final MAE` 相对同候选、同公共处理配置下的独立 reset FFT MAE 不得增加超过 2 BPM。它用于排除 HF/LMS 把本来可用的纯 PPG 结果明显处理坏的候选，不表示 Final MAE 必须低于 2 BPM。
_Avoid_: reset FFT 绝对精度门槛, 测试记录门控, 用另一候选的 FFT 结果比较

**最差训练记录目标**:
场景内三折训练阶段对同一候选先分别计算两条训练记录的严格运动段 Final MAE，再以其中较大值作为 BO 主目标；搜索后以全局最小主目标为锚点形成预声明容差带，带内再按训练平均误差等固定键排序，禁止一条记录的极低误差掩盖另一条记录失效或使用不传递的两两容差比较。
_Avoid_: 训练记录简单平均目标, 测试误差参与目标, 单记录最优

**局部邻域复核**:
在共享参数搜索后，对接近最优的中心候选逐一改变一个参数相邻一档并重放训练记录，用支持邻居比例和参数悬崖检查区分稳健参数平台与孤立最优点。邻域结果只能支持或否决已入围中心，不能让未经自身邻域验证的新点直接晋级。
_Avoid_: 额外无上限搜索, 测试后扩网格, 邻居最低点直接替换中心

**独立 BO 双基线**:
同时保留历史归档的旧 Lite 单记录 BO 作为实际精度锚点，并在同一代码、相同不重复候选预算和相同随机种子下重跑旧空间作为操作性对照。历史锚点回答新方案最终质量是否退化，同代码对照减少代码版本混杂，但多个空间结构和覆盖率同时变化时不能单独证明某一参数维度的因果效果。
_Avoid_: 只比较历史不同代码结果, 用同代码重跑覆盖历史锚点, 把操作性差异写成单维度因果效果, 把单记录 BO 当泛化结果

**开发复用数据 pilot**:
评价记录已经参与机制、空间或规则设计，但本轮仍在折内严格隔离测试记录的开发阶段评估；它用于筛选可冻结方案，不能提供未见数据上的确认性泛化结论。
_Avoid_: 独立确认实验, 未见数据验证, 最终泛化通过

**平滑机制人工门**:
正式独立 BO 和场景内三折之前，对统一心率平滑时长完成独立敏感性实验并交付逐记录统计、未来数据依赖和心率曲线；只有人工明确选择统一时长后才能进入正式搜索。若证据不支持统一时长，必须返回机制设计，不得让程序自动挑值后继续。
_Avoid_: 平滑时长继续混入主 BO, 自动 GO, 用正式测试结果回选平滑

**物理记忆长度**:
自适应滤波器抽头覆盖的实际时间跨度，用于在不同采样率下保持滤波记忆语义一致；相同抽头数不代表相同物理记忆长度。
_Avoid_: 跨采样率固定阶数, max_order 泛称, 只按抽头数比较

**确认性泛化阶段**:
在 HF 消噪机制和评价口径冻结后，使用未参与机制开发的个体检验跨场景与跨个体稳定性的后续阶段。
_Avoid_: 单样本机制调试, 反复修改阈值, 拟合上限实验

**场景条件化个体内算法**:
在同一个体内使用统一 HF 消伪影机制，并根据运行前已知的运动场景选择一套冻结参数；同一场景的所有记录必须使用完全相同的参数。它不表示算法能够自动识别场景，也不能作为无场景标签或跨个体泛化证据。
_Avoid_: 逐记录调参, 测试样本 BO, 全场景统一算法, 自动场景识别

**场景内三折评估**:
对同一场景的三条记录执行留一记录评估，每折使用两条记录按统一选参规则学习场景参数，并在剩余一条记录上验收。三折允许因训练记录不同得到不同参数，用于评价参数学习规则及稳定参数平台的场景内可迁移性；三折完成后另行形成一套供未来同场景记录统一使用的冻结参数。
_Avoid_: 三条记录共同调参后回代, 逐测试记录 BO, 三折参数必须数值相同, 未见个体验证

**全场景统一参数口径**:
同一个体的全部运动场景共用一套冻结参数的严格补充评估，用于衡量场景条件化收益对显式场景身份的依赖程度。
_Avoid_: 场景参数平均, 测试后择优配置, 跨个体通用参数

**机制门控效应**:
同一窗口频谱证据在不同算法机制开关下被采用或排除的差异，包括运动段低频重捕获、高频锁定逃逸、保护带和搜索范围状态对最终心率输出的影响。它用于隔离自适应滤波器本身的频谱差异和后处理状态机造成的差异。
_Avoid_: 后处理影响泛称, 参数差异混合解释

**机制隔离口径**:
保留完整时间线但固定心率追踪并关闭运动段重捕获与逃逸门控，只统计运动段自适应窗口，以单独归因 HF 消噪器频谱与下游结果差异的实验口径。
_Avoid_: 生产默认配置, 门控机制去留结论, motion-only 裁剪求解

**运动段低锁上跳重捕获**:
运动段中追踪状态连续锁在 50-80 BPM 等偏低区域时，若频谱中存在稳定、幅值足够、避开运动惩罚中心且位于有限修复走廊内的较高候选峰，算法尝试以受限步长上跳到该候选轨迹的补救机制。三窗确认使用双证据择一：候选峰自身不明显回落即可独立通过；候选明显回落时，才要求低锁轨迹的同步上升漂移作为旁证。它不要求所有真实候选都等待输出先恢复，也不是泛化的低频重捕获，主要面向开合跳等真实心率上升但状态被低频运动伪峰锁住的场景。
_Avoid_: 低频重捕获泛称, 任意远端候选跳转, 输出先上升门槛, 高频锁定逃逸

**真实上升证据**:
运动段低锁上跳重捕获的在线触发证据，表示当前窗口序列存在心率应从低锁区向更高候选轨迹迁移的非答案信号。它可以来自较高 PPG 候选峰的跨窗口稳定性、候选峰幅值竞争、运动段动态阶段或参考信号一致性，但不得使用参考心率、离线误差或 Lite/KLMS 对照结果作为在线触发条件。
_Avoid_: 参考心率门控, 事后误差修正, 单窗高峰冒尖

**上升确认观测**:
上升候选谱系请求重锚时提交的有界因果证据记录，区分上一窗追踪状态、前置选择候选、谱系候选、原始谱支持、候选来源及从目标获取到当前窗的证据历史。它只包含当前及历史运行时可得信息，不包含参考心率或离线误差。
_Avoid_: 确认参数包, 参考心率特征, 样本身份门控

**谱系证据片段**:
影子谱系目标从获取开始，到首次重锚授权、释放或终止为止的有界运行时证据序列。它用于评价谱系自身的上升形态、前置选择器采纳历史和运动伪影关系，不把重叠窗口当作独立样本。
_Avoid_: 单窗确认特征, 完整记录统计, 重叠窗口样本集

**前置选择候选**:
上升候选谱系介入前，既有候选选择与保护规则已经选出的当前候选；它可能不是原始谱最强峰，因此不应称为 raw top-1 或 legacy 峰。
_Avoid_: legacy candidate, 原始主峰, 最强谱峰

**重锚授权**:
上升候选谱系允许既有追踪链在普通限速前采用谱系候选的决定。它只改变前置选择结果，不拥有或直接写入 Final 心率。
_Avoid_: Final 改写, 心率硬切, 输出所有权

**原始谱支持比**:
谱系候选的原始局部峰幅值相对同窗最强有限正幅值局部峰的比例，用于描述候选在未惩罚谱中的相对支持。其窗口聚合方式和数值门槛属于版本化实验合同，不属于术语定义。
_Avoid_: 惩罚后排名, 任意 FFT bin 比值, 固定 0.45 门槛

**影子谱系目标所有权**:
上升候选谱系在有限运动窗口内对一个待确认候选目标的生命周期责任，包括获取、刷新、保持和释放；它不等于 Final 心率写入权，运动作用域结束或流结束时必须终止。
_Avoid_: Final owner, 隐式对象销毁, 无界候选缓存

**真实上升谱系正例**:
由参考心率仅在离线阶段确认、确实需要上升候选谱系保留或重锚才能避免漏跟的独立生理上升 episode。参数坐标副本和重叠窗口不得重复计为独立正例。
_Avoid_: 所有 reanchor cell, 不退化触发, 坐标级样本量

**上升确认冻结验收集**:
未参与当前上升确认规则或支持门定标、在规则和 episode 清单冻结后才运行的记录集合。它用于检查真实上升召回和防误杀，但不自动等同于全局未见数据或跨个体泛化集。
_Avoid_: 开发正例, 全局未见集, 阈值调参集

**上升机制外部压力集**:
未参与当前确认规则形成、但因历史训练复用或机制对照不足而不能与主验收证据同权的冻结记录集合。它只能在当前源身份下提供配对压力证据，不能用于定标或单独授权机制晋级。
_Avoid_: 独立验收集, 历史报告基线, 外部数据通过票

**上升 episode 冻结清单**:
在任何候选确认规则运行前，仅依据参考心率与运动作用域冻结的独立生理上升片段和非上升运动片段登记。它描述生理形态而不预先断言机制必须重锚，未检出上升的完整记录仍属于非回归证据。
_Avoid_: 重锚真值清单, 候选输出筛选, 无上升记录排除表

**谱系机制分级扩围**:
上升候选谱系先通过已知红绿单元与真实上升正例，再进入有界四场景面板，最后才允许完整矩阵的实验晋级顺序。完成当前级别不表示自动获准进入下一级。
_Avoid_: 一次性全矩阵, 首例失败即中断本级, 自动晋级

**上升谱系已知风险候选**:
在开合跳开发记录上恢复共享参数可行性、但在 Quanji 部分坐标发生假阳性重锚，且现有确认观测尚不能稳定区分正负事件的算法研究身份。它只能在默认关闭、风险显式登记和固定源码身份下参与离线选择器研究，不构成生产可用机制。
_Avoid_: 已修复上升机制, 四场景安全默认算法, Quanji 专用黑名单机制

**同场景跨记录泛化验证**:
参数选择规则与算法身份冻结后，仅使用同场景训练记录选择一个固定参数档位，再在未参与机制设计、选择规则形成或阈值定标的新记录上验收的泛化证据。开发复用记录上的留一重放只能作为选择器审计，不能替代该验证。
_Avoid_: 开发三折选择器重放, 三记录共同拟合, 测试记录 BO

**风险覆盖复算**:
在全量单次直接证据完成后，对所有产生后果性机制行为的单元和预先冻结的分层静默样本再次运行的确定性检查。它只能证明被复算区域的一致性，不能表述为全矩阵重复验证。
_Avoid_: 全矩阵三次复算, 结果后手选样本, 全量确定性证明

**相对证据门控**:
根据当前窗口及邻近窗口的谱峰竞争、候选稳定性、运动伪峰关系和历史轨迹一致性来判断机制是否触发，而不是主要依赖固定 BPM 阈值。它用于提高跨场景泛化能力，避免同一组绝对门槛在写字、键盘、握力计和开合跳中产生相反含义。
_Avoid_: 固定阈值堆叠, 样本特化参数, 只按绝对 BPM 裁决

**候选资格过滤**:
运动段低锁上跳重捕获中用于排除明显不安全上跳目标的第一层判断。它关注候选是否只是单窗冒尖、是否明显贴近运动伪峰或谐波、是否会让当前合理心率区域失去可达性，以及是否与窗口序列证据严重冲突。
_Avoid_: 综合分直接裁决, 最高峰优先, 只看候选 BPM

**候选相对评分**:
在候选资格过滤之后，对合格上跳候选进行排序的证据聚合过程。它比较候选的跨窗口稳定性、幅值竞争、与历史轨迹的相对关系、与 HF/ACC 参考信号的冲突程度，以及触发后搜索范围是否仍保持合理可达。
_Avoid_: 黑盒总分, 单一幅值排序, 样本特化权重

**可达性修复**:
机制触发后优先调整搜索范围、追踪中心或候选可采纳性，使合理候选峰重新进入算法可达区域，而不是直接把最终心率改写为目标候选。它用于把运动段低锁上跳重捕获从输出补丁转为追踪状态恢复机制。
_Avoid_: 直接改写 Final HR, 单步硬切, 候选峰强制采用

**ACC 对比参考信号**:
用于在下游心率估计任务中对照热式界面传感器 HF 信号去除运动伪影能力的运动参考信号。它复用 HF 主链路确定的评估口径，不单独以提升 ACC 性能为目标进行参数优化。
_Avoid_: HF 链路触发证据, ACC 独立优化对象, ACC 辅助门控

**HF 主链路**:
以双侧热式界面传感器信号为运动伪影参考、承担本项目算法优化与跨个体泛化验证的主要心率估计路线。
_Avoid_: 多参考信号等权主线, HF/ACC 融合路线

**双侧 HF 联合参考**:
将 PPG 两侧的热式界面传感器视为共同描述局部界面动态的多通道参考，而不是彼此独立或只保留相关性最高的一路。
_Avoid_: 单侧 HF 代表, 两路独立主线, 最高相关通道替代

**正则化多输入 NLMS**:
联合使用双侧 HF 参考、按参考能量归一化更新并约束相关通道权重稳定性的线性自适应消噪主方案。
_Avoid_: 相关性排序串行 LMS, 单路硬选择, 默认非线性滤波

**HF 可解释性驱动的双侧分频消伪**:
按频带评估 PPG 成分能否被双侧 HF 解释，并依据左右通道质量、共线程度和 HF—PPG 映射稳定性约束各通道的消除贡献；只有得到因果 HF 证据支持的竞争成分才允许较强消除。参考心率只用于离线验收，不得用于运行时选择或保护频带。
_Avoid_: 双侧强制等权输入, 全频带统一强度消除, 参考 HR 频带保护, 复杂 VSS/W2 状态机

**可在线化核心**:
HF 自适应消噪与心率追踪的运行时核心只依赖当前窗口及过去状态，不使用未来窗口或参考心率；离线研究路径可保留零相位预处理，但不能据此声称端到端实时部署。
_Avoid_: 已嵌入式部署, 端到端在线系统, 使用未来信息的运行时核心

**离线训练 BO**:
只使用训练样本及其参考心率搜索可跨样本复用的共享参数，并在测试或运行阶段固定应用的参数选择过程。
_Avoid_: 在线运行时 BO, 测试样本调参, 单样本拟合上限

**测试样本 BO**:
使用被评估样本自身的参考心率选择参数的样本内优化，只表示该样本的拟合上限，不能作为跨样本或跨个体泛化证据。
_Avoid_: 跨人泛化结果, 部署参数验证, 无监督参数自适应

**旧 Lite 场景三折基线**:
旧 Lite 机制在场景内三折评估中，仅使用同场景另外两条记录及其参考心率选择共享参数，再在留出记录上固定运行的公平基线。它与新机制使用相同的数据隔离规则，用于判断机制和参数学习规则是否产生可迁移收益。
_Avoid_: 旧 Lite 单记录 BO, 历史最优曲线直接比较, 测试记录调参

**旧 Lite 单记录 BO 拟合上限**:
旧 Lite 使用每条被测记录自身参考心率独立 BO 得到的样本内能力上限。它必须与公平三折基线同时报告，但只用于衡量新方案距离当前样本内最佳工程能力的差距，不能作为基础机制成功的唯一门槛。
_Avoid_: 旧 Lite 泛化基线, 可部署旧 Lite, 公平冻结对照

**基础机制成功**:
新 HF 消伪影机制通过频谱证据门槛和下游任务门槛，并在场景内三折评估中稳定优于旧 Lite 场景三折基线。
_Avoid_: 只在开发记录改善, 只凭平均 MAE, 击败单记录 BO 才算成功

**样本内能力上限突破**:
通过基础机制成功的新方案进一步达到或优于旧 Lite 单记录 BO 拟合上限的强结果。它表示在当前记录上超过历史 Lite 的样本内调参能力，不自动构成跨场景或跨个体泛化证据。
_Avoid_: 基础成功门槛, 跨人优越性, 无答案在线算法

**下游任务基线**:
在相同心率估计任务和评价口径下，用于显现 HF 主链路去除运动伪影有效性的 ACC 或纯 FFT 对照路线，而不是需要分别优化到最优性能的并列主算法。
_Avoid_: 竞争主算法, 独立调参基线, 传感器本体指标

**低锁上跳三层验收**:
评估运动段低锁上跳重捕获优化时的优先级口径：先确认写字、键盘、握力计等防误触发场景不再大量制造不可达窗口，再确认开合跳、波比等历史救援场景仍能摆脱低锁，最后才比较总体 MAE、hit rate 和触发统计。
_Avoid_: 只看总体 MAE, 完全禁用机制的假胜利, 单一救援样本过拟合

**失败快速退出**:
运动段低锁上跳重捕获触发后，若候选连续失稳、上跳后候选不再合格、搜索范围持续远离合理区域，或输出被推向疑似谐波/运动伪峰区，机制应快速退出并回到普通追踪。它承认在线证据可能误判，防止错误状态污染后续窗口。
_Avoid_: 长时间保持错误重捕获态, 无撤销硬切, 过长冷却导致漏救援

**整段求解运动段评估**:
算法在完整样本时间线上运行，以保留运动前历史、运动段状态和阶段切换连续性，但实验指标只统计运动段自适应链路窗口。它用于避免 motion-only 裁剪求解改变进入运动段时的追踪状态。
_Avoid_: motion-only 求解, 全段指标泛称

**运动感知纯 FFT 基线**:
只使用 PPG 原始频谱及其历史轨迹估计心率、但允许感知运动段和运动后阶段边界来管理追踪状态的纯 FFT 对照路线。它不得使用自适应滤波输出、运动参考去伪影结果、参考心率或答案派生信号作为目标或门控证据。
_Avoid_: continuous FFT 泛称, 自适应辅助 FFT, 重捕获专用 FFT

**运动后保护窗**:
运动段结束后的一段过渡时间，算法仍需防止从运动链路直接切回静息链路造成不合理跳变。保护窗长度是需要通过实验评估的设计变量。
_Avoid_: 恢复段, recovery

**legacy 运动后动态保护窗**:
PM-CHR 之前由 adaptive 与 reset FFT 的差值和连续窗口规则直接决定 Final 回切的历史策略。PM-CHR 启用时它只保留兼容诊断，不再拥有 Final 写入权。
_Avoid_: PM-CHR, 当前默认策略, 并列 Final 控制器

**稳定重捕获保护窗长**:
运动结束后等待 PPG 波形大概率摆脱运动伪影主导、足以支持纯 FFT 重新捕获心率的全局默认保护窗长度。它用于评选运动感知纯 FFT 基线，不表示按样本动态调节的门控延迟。
_Avoid_: 最优切换延迟, 样本自适应保护窗, recovery 固定时长

**运动后静息 FFT 重捕获**:
运动后保护窗结束后的静息状态，此时 PPG 波形若已足够干净，应允许 FFT 主频重新捕获心率，并使用适合该阶段的心率变化限幅策略。
_Avoid_: 运动后 rest, 后恢复段, 静息恢复段

**运动后因果交接恢复（PM-CHR）**:
v2 整段求解在存在参考链路和运动段时采用的默认运动后策略。它只使用当前及历史 PPG、切换前 Final 和数据质量等因果信息，把 Final 从 adaptive 一次性交接给交接 reset，并由单一切换器保持控制。
_Avoid_: 双 reset 算法, 动态保护窗, gap rescue 补丁, 参考心率门控

**reset FFT 链路**:
运动结束后清空或弱化既有追踪历史、重新从 PPG 频谱候选中捕获心率的链路族，包括独立 reset FFT 与交接 reset FFT。两者共享 raw PPG 频谱证据，但具有不同的初始化信息边界。
_Avoid_: 普通 FFT 曲线, 静息 FFT 泛称, 单一 reset 状态

**独立 reset FFT**:
只使用 raw PPG 频谱及自身 reset 后状态重新捕获心率的纯 PPG 对照链路，不读取自适应链路或 Final 的历史。它是主报告中 FFT 对照曲线的权威来源，不参与 Final 的切换决策。
_Avoid_: 交接 reset FFT, continuous FFT, Final 辅助 FFT

**交接 reset FFT**:
使用与独立 reset FFT 相同的 raw PPG 频谱证据，但在启动阶段允许将切换前 Final 及其因果下降趋势作为弱先验的重捕获链路。它只产生可供统一切换器评估的 reset 目标，不直接改写 Final。
_Avoid_: 独立 reset FFT, 自适应滤波输出, Final 直接改写

**交接候选稳定性证据**:
交接 reset 的 selected raw 候选依据峰可见性、幅值竞争、跨窗稳定性和轨迹身份连续性形成的诊断事实。它只回答候选峰是否可信，不单独形成切换控制状态。
_Avoid_: candidate qualification 控制器, gap 即可信, 参考心率门控

**交接 tracker 收敛证据**:
交接 tracker 的公开输出已经追到稳定 raw 候选附近且未依赖 held previous 的诊断事实。它只回答执行轨迹是否追上目标，不单独授予 Final 写权限。
_Avoid_: tracker ready 控制器, selected 峰已正确, 输出自然可信

**交接目标可消费**:
交接候选稳定性证据与交接 tracker 收敛证据同时成立时形成的唯一切换许可。统一切换器只消费该许可；两个诊断事实不再各自维护 Final 控制状态。
_Avoid_: candidate qualified, switch target ready, bootstrap admissible, gap 即许可

**一次性 PPG 启动门**:
完整心率窗口全部位于运动结束后，且连续 raw PPG 序列重新表现出可用心搏周期性与谱峰连续性时打开的单向启动边界。打开后不再维护丢失、恢复或固定超时状态。
_Avoid_: 持续可观测状态机, 固定等待结束, 参考心率质量门控, reset ready

**A2 一次性恢复初始化**:
曾在实验中于 PPG 可观测性恢复后重建交接 tracker 的历史候选。当前 PM-CHR 不采用该路径，而使用连续运行的交接 tracker 与受控重锚。
_Avoid_: 当前默认重启, 独立 reset 重启, PM-CHR 必经状态

**交接 reset 受控重锚**:
交接 reset 候选取得充分且连续的无参考 PPG 证据后，将 handoff tracker 的内部追踪状态迁移到该候选轨迹附近，以解除旧低锁状态和方向限速造成的长期不可达。重锚不直接改写 Final，也不等同于取得切换目标就绪。
_Avoid_: Final 硬切, 参考心率校正, 单窗 top-1 强制采用, 输出补丁

**运动后 Final 单写入权**:
运动后只有统一切换器可以改变 Final 的来源；独立 reset、交接 tracker、legacy dynamic guard 和诊断探针只能提供证据或目标。任何并列 Final 消费入口都违反该边界。
_Avoid_: 双控制器, dynamic guard 直接消费, tracker 直接写 Final

**运动后自然不切换**:
一次性 PPG 启动门或交接目标可消费始终未成立时，Final 自然保持 adaptive 的结果。它不是限时弃权状态，也不阻止较晚出现的合法交接。
_Avoid_: permanent safe abstain, confirmation timeout, no-switch 失败泛称

**运动后不可逆交接**:
统一切换器一旦正式采用交接目标，本次运动后阶段不再返回旧 adaptive Final；后续目标若暂时不可用或与上一已接受 Final 的身份不连续，则由同一切换器保持上一值，不产生新的回退控制器。
_Avoid_: post-switch hold 开关, ready 撤销后回跳, 重复交接, 第二写入者

**因果暂接**:
正式交接目标尚不可消费时，基于 raw PPG 候选与切换前 Final 弱先验形成的有限暂时接管。它只能输出 tracker 实际目标，不得沿差值外推，并在正式目标可消费或证据被明确撤销时结束。
_Avoid_: 参考心率引导, provisional 外推, 无界补偿, 正式交接

**运动后绝对应用门槛**:
用于判定失效样本是否真正被交接 reset 救回的绝对性能要求；目标层和切换后 Final 的固定 60 s MAE 应不高于 3 BPM，不能以相对改善率替代。E20 继续逐窗审计，但不单独否决生理连续且总体 MAE 达标的结果。
_Avoid_: 改善 50% 即可用, D1 均值门槛, 正常样本统一 3 BPM 门槛

**连续纯 FFT 诊断链路**:
只使用 raw PPG 频谱并保持自身全程追踪历史、不在运动后重新初始化的离线诊断对照。它不作为主报告的默认 FFT 曲线，也不进入 Final。
_Avoid_: 独立 reset FFT, 交接 reset FFT, 默认纯 FFT 基线

**自适应链路**:
使用 HF、ACC 或其他参考信号经自适应滤波后得到的心率估计链路。PM-CHR 正式交接前，Final 默认沿用该链路；正式交接后不再返回。
_Avoid_: adaptive 泛称, LMS 曲线泛称

**旧 Lite BO 对照**:
历史 Lite 预设批处理输出中保存的每样本 BO 最优参数、最终心率曲线和报告文件。它是评估新运动后机制时的同源基线，不应只被当作一个聚合均值。
_Avoid_: Lite 均值基线, 固定 Lite source

**同源替换实验**:
在运动段和运动后保护窗的源曲线口径保持一致时，只替换运动后保护窗结束之后的重捕获后段，以区分源参数差异和重捕获机制本身的效果。
_Avoid_: 固定 source 对比, 混合口径 delta

**重捕获后段**:
从运动后保护窗结束后开始、到样本结束或下一段运动开始前的算法输出阶段。它是 reset FFT、首窗峰选择、边界平滑和短期 fallback 的评估对象。
_Avoid_: 整个运动后段, source 段

**首窗峰共识**:
运动后静息 FFT 重捕获开始后，使用连续多个窗口的候选谱峰稳定性来确认首个可接受心率轨迹，而不是只信任单个窗口的最大谱峰。
_Avoid_: 单窗主峰, raw peak 直接采用

**运动后追踪策略族**:
专门用于运动后静息 FFT 重捕获的心率追踪与限幅策略集合。它独立于普通静息段策略，具体参数应由实验或预设选择，并应支持上升保守、下降放宽的非对称限幅。
_Avoid_: 复用普通 rest 参数

**亚硬切确认交接**:
交接目标可消费且与当前实际 Final 的差小于硬切边界时，连续两个窗口确认后由统一切换器正式接管。兼容 trace 仍使用 `stable_crossover`，但不再表达旧的 6 BPM“稳定交汇”分界。
_Avoid_: 单点相交, 6 BPM 交汇门槛, 内部 adaptive 交汇, 永久无动作区

**高差快速交接**:
交接目标已经可消费且与当前实际 Final 的差达到硬切边界时，统一切换器在当前窗口立即正式接管，以快速纠正运动段遗留的高漂移。高差只决定执行速度，不授予目标可信度。
_Avoid_: 持续高差计数, gap-only rescue, 无资格硬切, adaptive_rising_rescue

**运动后静息段指标**:
只统计运动后保护窗结束后、到样本结束或下一段运动开始前的心率估计误差与命中率。它是评估运动后静息 FFT 重捕获是否成功的主指标。
_Avoid_: 只看 total/rest/motion 汇总

**运动后分池防退化**:
失效样本池与正常样本池分别汇总运动后误差；正常池平均 MAE 回归受限，单条明显回归必须解释，失效池收益不得抵消正常池整体退化。E20 是审计项，非生理反向跳变是连续性硬失败。
_Avoid_: 全 24 条单一均值, 失败收益抵消正常退化, E20 单独一票否决

**固定 60 s post-motion MAE**:
从运动结束点开始固定取后续 60 秒，计算 Final 心率与参考心率的平均绝对误差。它用于观察恢复早期，避免长静息尾段稀释运动刚结束后的失败。
_Avoid_: 完整 post-motion MAE, 切换后 MAE

**交接重捕获初始化**:
交接 reset FFT 启动时，将切换前 Final 及其因果下降趋势作为候选排序与可达性判断的弱先验，同时要求输出仍由 raw PPG 候选证据产生。
_Avoid_: 直接继承保护窗末端 HR, Final 直接改写, 强历史约束

**静息段**:
未处于运动段，也不属于运动后保护窗的低运动状态。运动前静息与运动后静息可能需要不同的追踪和限幅策略。
_Avoid_: rest 泛称

**窗口阶段**:
报告和诊断中描述窗口所属生理与算法阶段的分类，例如运动前静息、运动段、运动后保护窗和运动后静息 FFT 重捕获。它比旧的 rest/motion/recovery 分类更精确。
_Avoid_: window_kind 旧三分类

**机制级路线审计**:
以一次算法机制变更为最小审计单元，串联动机、实现、实验输出、窗口诊断、泛化收益、退化风险和去留建议。
_Avoid_: 只看提交列表, 只看最终 AAE

**外部泛化样本**:
cross_person 评估中不参与共享 BO 参数训练、只用于复核跨个体泛化表现的被试外样本。
_Avoid_: 测试集泛称, own_train

**冻结压力评估集**:
失效现象已经被观察、但在机制和阈值冻结前不参与设计、筛选或调参，只在冻结后影响 GO/NO-GO 的记录集合。它可验证已知失效是否跨个体改善，但不能被表述为完全未见泛化集。
_Avoid_: 开发集, 调参集, 完全未见测试集, 只展示样本

**v2 默认运动后回切机制**:
即 PM-CHR。Final 先沿用 adaptive，允许有限因果暂接，并仅在交接目标可消费后通过亚硬切确认交接或高差快速交接完成不可逆切换；独立 reset 和 legacy dynamic guard 不参与 Final 决策。
_Avoid_: Lite 特例开关, GUI 隐式修复, 固定 20s 默认回切, 双写入者

**权重状态策略**:
描述自适应滤波器权重在相邻心率窗口之间如何管理的实验因素。至少区分独立窗口权重与连续流权重；跨窗口方案必须只处理新到达的样本一次，不能因窗口重叠而重复训练旧样本。
_Avoid_: 跨窗状态, 是否继承权重, 默认连续权重

**独立窗口权重**:
每个心率窗口开始时将自适应滤波权重重置，再仅使用该窗口内的数据完成适应与输出；用于隔离单窗口内参考信号的去伪影能力。
_Avoid_: 每窗清零, 冷启动窗口, reset 模式

**连续流权重**:
首个窗口完成初始化后，在后续窗口之间保留自适应滤波权重，并且每次窗口推进只用新到达的非重叠数据更新权重；用于模拟真实在线数据流中的持续学习。
_Avoid_: 跨窗继承, warm start, 状态保留模式

**固定 HF 前视**:
所有 NLMS 阶数条件共享的最大正向时间对齐范围，本轮固定为 200 ms；物理记忆长度只表示向历史方向的覆盖，使对齐能力不随阶数一起变化。
_Avoid_: 非因果时延范围, 阶数自带延迟, 双向记忆长度

**共享因果 HF 归一化**:
双侧 HF 各自使用按时间顺序持续更新的幅值归一化状态，并让独立窗口与连续流权重实验共享同一归一化输入；每个原始样本只参与一次统计量更新。
_Avoid_: 每窗 z-score, 权重模式专属归一化, 重叠窗口重复归一化

**相对 NLMS 正则化**:
按双侧 HF 输入向量维数缩放 NLMS 分母保护项的定义，使不同物理记忆长度下的正则化相对强度保持可比。
_Avoid_: 固定绝对 epsilon, 阶数相关分母保护, 正则化常数泛称

**权重半衰期**:
描述泄漏 NLMS 中既有权重衰减到一半所需的物理时间，用于跨采样率比较旧运动模式的遗忘速度；无穷大表示不泄漏。
_Avoid_: 逐点泄漏系数, lambda 泛称, 权重衰减率

**共差模 HF 表示**:
将归一化后的双侧 HF 正交变换为共同变化分量与左右差异分量的参考表示，用于分析对称结构中的共同运动和局部形变；它与原始双侧表示包含相同线性信息。
_Avoid_: 新增两路 HF, raw 加 sum/difference, 数据驱动翻转

**三维频谱证据**:
分别从 HF 支持的伪影抑制、真实心率峰暴露和脉搏频谱保留三个维度验收自适应滤波结果，不预先压缩为单一加权分数。
_Avoid_: 频谱干净度总分, 单指标频谱收益, 只看峰值排名

**参考心率邻域**:
围绕窗口参考心率定义的频谱保护与评价范围，主分析采用正负 10 BPM，并以正负 7.5 和 15 BPM 检查带宽敏感性。
_Avoid_: 单个参考频点, FFT 补零分辨率, 任意心率保护带

**双侧 HF 频谱软并集**:
由两路归一化 HF 功率谱在每个频率上的较强响应构成的连续伪影支持权重；任意一侧捕捉到的局部运动都能进入评价，但不需要硬选伪影峰。
_Avoid_: HF 峰值清单, 双侧谱简单拼接, 硬阈值伪影频带

**记录级统计单位**:
将一条完整采集记录视为本轮扩大实验的最小独立单位；重叠窗口只用于记录内机制诊断和汇总，不作为彼此独立的统计重复。
_Avoid_: 窗口级样本量, 重叠窗口独立检验, CGX 跨人统计

**双哨兵非对称准入**:
候选必须在困难哨兵上产生机制与心率收益，同时将简单哨兵作为硬性防退化约束；不得用两条记录的平均分掩盖任一侧失败。
_Avoid_: 双样本平均排名, 困难样本单独最优, 简单样本补偿项

**E10/E20 尾部风险**:
分别统计心率绝对误差超过 10 BPM 和 20 BPM 的窗口，用于定位严重失锁及其发生阶段。它们是 MAE 的逐窗审计，不单独等同于非生理切换或自动 NO-GO。
_Avoid_: 大误差泛称, E20 一票否决, 尾部总分

**运动后反向跳变**:
运动后 Final 单窗下降至少 20 BPM，并在随后 5 个窗口内相对低点回升至少 20 BPM 的非生理往返事件。合法的一次性硬切不属于反向跳变；任何候选出现该事件都属于连续性硬失败。
_Avoid_: 最大单窗跳变, E20 窗口, 正常恢复波动

**双口径权重状态比较**:
同时报告 W0/W1 在相同滤波参数下的权重状态配对效应，以及两种状态分别从稳定参数平台配置后达到的最佳可达性能；前者是主机制证据。
_Avoid_: 只比各自最优, 强制共享最终参数, 权重继承总收益

**稳定参数平台**:
在记忆长度与步长响应面上连续通过机制和防退化门槛的二维区域，最低要求包含一个完整的相邻二乘二网格；孤立点或单轴细线不属于平台。
_Avoid_: 单点最优, 若干优秀参数, 热图高值区

**规则化网格细化**:
粗网格完成后按预先声明的内部加密、边界扩展或失败诊断规则执行的唯一一次补充搜索；它不得演变为围绕哨兵样本反复追逐最优点。
_Avoid_: 迭代调参, 人工扩网格, 隐性 BO

**稳定性全因子**:
在稳定参数平台代表上联合考察相对正则化、权重半衰期和双侧 HF 通道表示的第二阶段实验，用于识别这些稳定化机制的主效应与交互效应。
_Avoid_: 单因素稳定性调参, 第二阶段 BO, 稳定机制逐项最优

**W2 因果证据边界**:
门控连续权重只能依据当前及历史的 PPG、双侧 HF、残差、权重状态和数据质量采取动作；人工运动标签与参考心率仅用于离线评价。
_Avoid_: 标签驱动重置, 参考心率门控, 回看式状态切换

**W2 分级动作**:
门控连续权重按证据强度依次采用正常更新、暂时冻结、泄漏遗忘和完全重置，分别处理有效学习、短暂无证据、映射持续变化和旧状态失效。
_Avoid_: 单阈值清零, 冻结重置泛称, 频繁硬切权重

**HF 证据驱动 VSS**:
由双侧 HF 活动、伪影可解释性和更新一致性等因果证据生成有界 NLMS 步长的项目定制变步长机制；它不使用参考心率或人工运动标签。
_Avoid_: BO 步长, 参考心率驱动步长, 黑箱 VSS

**V4 三证据**:
HF 证据驱动 VSS 中分别表示双侧 HF 活动度、残余可解释性和梯度一致性的三个有界分量，并通过 A、AR、ARG 嵌套消融验证各自贡献。
_Avoid_: V4 综合质量分, 可训练证据权重, 不可拆解步长特征

**VSS 步长包络**:
以稳定固定步长为中心定义的有界相对调制范围，用于比较保守、平衡和宽范围的在线步长变化，而不重新搜索样本专属绝对步长。
_Avoid_: VSS 上下界 BO, 独立绝对步长范围, 无界变步长

**V4 共享证据时间尺度**:
HF 活动度、残余可解释性和梯度一致性共同使用的物理平滑时长，用于限制 V4 自由度并在不同采样率下保持相同响应速度。
_Avoid_: 每证据独立时间常数, 步长额外平滑, 采样点平滑长度

**原则冻结与数值候选**:
实验架构、因果信息边界、阶段顺序和验收逻辑作为稳定原则；具体网格、阈值与时间常数只是首轮候选，可依据响应面和失败证据版本化修订。
_Avoid_: 候选值即最终参数, 结果后随意调参, 每样本专属修订

**首次冻结扩大验证**:
在查看 CGX 24 样本整体结果前冻结主方案与备选方案并统一运行的首次扩大检查；若其结果被用于修改算法，后续轮次必须标记为开发反馈而非未见验证。
_Avoid_: CGX 边跑边调, 扩大集独立验证泛称, 失败样本专调

**最终证据矩阵**:
由纯 PPG、冻结 ACC、单侧 HF、双侧固定步长、双侧变步长和双侧完整状态方案构成的对照集合，分别回答参考信号、双侧结构和算法机制的增量价值。
_Avoid_: 算法排行榜, ACC 独立优化矩阵, 只比最终方案

**增量复杂度保留**:
只有同时产生稳定频谱证据、下游心率增量并通过防退化门槛的附加机制才进入最终算法；没有稳定增量时回退到更简单候选。
_Avoid_: 为创新强留机制, 最复杂方案默认胜出, 只按平均 AAE 保留

**双机制联合扩大验证身份**:
在冻结源码上同时启用 `rise_candidate_lineage_enable=true`、`rise_confirmation_policy_id=legacy_v1` 和 `penalty_candidate_id=suppressed_protected_continuous_visibility_v1` 的实验算法身份。它用于扩大开发筛查，不等于生产默认；两个机制必须在每个候选坐标中同时保持开启。
_Avoid_: 当前生产默认, 只开 rise lineage, 只开 continuous visibility, 坐标间切换机制

**逐记录全段 MAE 时延微调**:
一条记录的求解参数和预测轨迹冻结后，在共同有效可靠窗口上比较 `4.0/4.5/5.0/5.5/6.0 s` 五个评价偏置，先确定全段 MAE 原始最小者；若该偏置的经典门槛未通过、而同一记录上一轮 R-all 偏置重新评价后通过，则正式有效偏置回退到上一轮值。它是读取原始参考心率的离线结果对齐步骤，不进入 BO trial、不改变求解轨迹，也不承担生理时延解释；原始最小者、有效偏置和回退原因必须分别留痕。
_Avoid_: 静息标定评价时间偏置, BO 时延维度, trial 内偏置选择, 求解器时延补偿, 在线时延估计

**逐记录 physical4D BO-120**:
在冻结四维物理空间中为一条记录运行三个独立 TPE repeat，每个 repeat 40 个逻辑 trial，种子为 `42/43/44`、前 10 个为 startup；重复建议保留为逻辑 trial 并命中内容缓存。它是小预算潜力筛查，不是 120 个不同坐标或 300 点完整响应面。
_Avoid_: 120 unique, 完整响应面, 三个 repeat 共享学习, 重复 trial 重算求解

**按场景质量与性能筛选的跨个体面板**:
先按冻结质量与经典误差门筛选逐记录候选，再在每个运动场景内选取来自不同受试者的记录，优先六人、最低五人且必须包含算法开发个体 LYX。该面板用于后续跨个体方法开发，因记录和 LYX 已参与开发且按性能筛选，不能称为随机人群、独立外部验证集或已经完成的 LOSO 实验。
_Avoid_: 随机队列, 独立测试集, LOSO结果, 未见个体泛化证据
