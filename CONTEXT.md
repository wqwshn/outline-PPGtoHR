# PPG Heart Rate Algorithm

This context defines the domain language used when discussing PPG heart-rate estimation across rest, motion, and post-motion periods.

## Language

**动态追踪算法预设**:
v2 心率求解中用于组织不同心率追踪策略、BO 使用范围和固定参数层级的算法方案。
_Avoid_: 算法模式泛称, 参数模板

**v2 运行策略束**:
由动态追踪算法预设和运行配置共同派生的策略集合，用来描述一次 v2 心率求解中的追踪、回切、限幅和保护窗规则。它把策略语义从兼容参数记录中分离出来，供各算法模块一致消费。
_Avoid_: 参数散列表, 单个预设名称

**Lite**:
固定静息、运动和恢复追踪参数的小搜索空间 v2 算法预设，主要用于效率基线和 TraceRescue 候选状态内核。
_Avoid_: 简单版泛称, 无 BO 算法

**TraceRescue**:
样本级运行多个固定候选状态，并用无监督轨迹诊断选择最终状态的 v2 算法预设。
_Avoid_: 在线状态机, 多数投票算法

**运动段**:
PPG 信号受到显著运动伪影影响、算法需要借助运动参考信号或运动感知策略估计心率的时间段。
_Avoid_: motion 区间, 运动窗口

**运动段高频锁定**:
运动段中真实心率峰仍存在于候选谱峰里，但谱峰追踪状态被更高频的运动伪峰、运动谐波或受保护的错误轨迹持续吸引，导致心率估计偏高。
_Avoid_: 高频漂移泛称, 运动后高漂移

**高频锁定逃逸**:
运动段高频锁定被连续证据确认后，算法放宽普通运动段的生理限幅约束，使用独立且更激进的下降策略逐步回到稳定的较低候选心率轨迹。
_Avoid_: 直接切低, 普通运动段限幅

**窗口失效原因分类**:
窗口诊断中用于描述心率估计失效主因的标签体系。每个窗口应有唯一主因，并可附加多个辅助标签，以便跨样本统计典型失效模式。
_Avoid_: 临时 debug 字段, 只看误差大小

**窗口失败主因**:
窗口失效原因分类中的唯一主标签，用于说明该窗口最终心率估计偏离参考心率的最主要机制。它与辅助标签共同使用，避免同一窗口在主统计中被重复计数。
_Avoid_: 多主因并列, 只按误差大小排序

**窗口 trace**:
每个心率估计窗口随结果保存的决策事实记录，描述候选峰、追踪范围、惩罚/保护状态和输出路径来源。它是窗口诊断决策语义的权威来源。
_Avoid_: 本地重放结论, 临时 debug summary

**真实峰可见性**:
自适应滤波后、进入后处理前的窗口频谱中，参考心率附近是否存在可用候选峰及其排名、相对幅值和与伪峰的分离程度。它只评价频谱证据本身，不包含追踪限幅、重捕获或最终输出是否采用该峰。
_Avoid_: 频谱干净度泛称, 最终误差反推峰存在

**真实峰可达性**:
真实心率峰在当前窗口是否能被算法状态实际采用，包括是否落入搜索范围、是否被历史心率、保护带、重捕获状态或高频锁定逃逸状态排除。它用于区分“峰不存在”和“峰存在但链路状态不可达”的失败模式。
_Avoid_: 谱峰命中率泛称, 只看候选峰排名

**频谱证据门槛**:
HF 自适应消噪候选必须先以尺度不变指标证明真实心率峰得到保留或增强、运动伪峰得到抑制且残余伪影关联降低，才有资格进入最终心率性能比较。未通过该门槛的候选不能仅凭最终 MAE 改善晋级。
_Avoid_: 只凭 Final HR 误差判断滤波质量, 整体压低频谱, 单一峰可见率

**下游任务门槛**:
通过频谱证据门槛的候选还必须在固定心率追踪口径下改善运动段误差、命中率和失锁风险，才能视为完整算法收益；只有频谱改善而无下游改善时，归入滤波器—追踪器接口诊断，不视为本轮算法成功。
_Avoid_: 只看平均 AAE, 用追踪后处理替代频谱改善, 只看代表样本

**机制发现阶段**:
使用单样本、单场景或单个体数据快速验证双侧 HF 联合参考如何改善运动伪影消除与真实心率峰暴露的探索阶段。
_Avoid_: 跨人泛化定稿, 最终论文效应量, 锁定确认实验

**分阶段机制 DOE**:
依次研究阶数与固定步长、稳定性机制、数据驱动变步长和冻结扩展效果的实验组织方式，每阶段只将通过两级门槛的稳健候选传给下一阶段。
_Avoid_: 一次性全参数 BO, 全组合无筛选穷举, 只保留最优试验

**稳健参数平台**:
在相邻参数组合及多个样本上持续通过频谱和下游任务门槛的参数区域，优先级高于单个样本上的最低误差点。
_Avoid_: 单点最优参数, BO 最佳 trial, 样本特化参数

**物理记忆长度**:
自适应滤波器抽头覆盖的实际时间跨度，用于在不同采样率下保持滤波记忆语义一致；相同抽头数不代表相同物理记忆长度。
_Avoid_: 跨采样率固定阶数, max_order 泛称, 只按抽头数比较

**确认性泛化阶段**:
在 HF 消噪机制和评价口径冻结后，使用未参与机制开发的个体检验跨场景与跨个体稳定性的后续阶段。
_Avoid_: 单样本机制调试, 反复修改阈值, 拟合上限实验

**场景条件化个体内算法**:
在同一个体内使用统一 HF 消伪影机制，并根据运行前已知的运动场景选择一套冻结参数；同一场景的所有记录必须使用完全相同的参数。它不表示算法能够自动识别场景，也不能作为无场景标签或跨个体泛化证据。
_Avoid_: 逐记录调参, 测试样本 BO, 全场景统一算法, 自动场景识别

**场景内三折评估**:
对同一场景的三条记录执行留一记录评估，每折使用两条记录按统一选参规则学习场景参数，并在剩余一条记录上验收。三折允许因训练记录不同得到不同参数，用于评价参数学习规则及稳定参数平台的场景内可迁移性；三折完成后另行形成一套供未来同场景记录统一使用的冻结参数。
_Avoid_: 三条记录共同调参后回代, 逐测试记录 BO, 三折参数必须数值相同, 未见个体验证

**全场景统一参数口径**:
同一个体的全部运动场景共用一套冻结参数的严格补充评估，用于衡量场景条件化收益对显式场景身份的依赖程度。
_Avoid_: 场景参数平均, 测试后择优配置, 跨个体通用参数

**机制门控效应**:
同一窗口频谱证据在不同算法机制开关下被采用或排除的差异，包括运动段低频重捕获、高频锁定逃逸、保护带和搜索范围状态对最终心率输出的影响。它用于隔离自适应滤波器本身的频谱差异和后处理状态机造成的差异。
_Avoid_: 后处理影响泛称, 参数差异混合解释

**机制隔离口径**:
保留完整时间线但固定心率追踪并关闭运动段重捕获与逃逸门控，只统计运动段自适应窗口，以单独归因 HF 消噪器频谱与下游结果差异的实验口径。
_Avoid_: 生产默认配置, 门控机制去留结论, motion-only 裁剪求解

**运动段低锁上跳重捕获**:
运动段中追踪状态连续锁在 50-80 BPM 等偏低区域时，若频谱中存在稳定且幅值足够的较高候选峰，算法尝试以受限步长上跳到该候选轨迹的补救机制。它不是泛化的低频重捕获，主要面向开合跳等真实心率上升但状态被低频运动伪峰锁住的场景。
_Avoid_: 低频重捕获泛称, 任意远端候选跳转, 高频锁定逃逸

**真实上升证据**:
运动段低锁上跳重捕获的在线触发证据，表示当前窗口序列存在心率应从低锁区向更高候选轨迹迁移的非答案信号。它可以来自较高 PPG 候选峰的跨窗口稳定性、候选峰幅值竞争、运动段动态阶段或参考信号一致性，但不得使用参考心率、离线误差或 Lite/KLMS 对照结果作为在线触发条件。
_Avoid_: 参考心率门控, 事后误差修正, 单窗高峰冒尖

**相对证据门控**:
根据当前窗口及邻近窗口的谱峰竞争、候选稳定性、运动伪峰关系和历史轨迹一致性来判断机制是否触发，而不是主要依赖固定 BPM 阈值。它用于提高跨场景泛化能力，避免同一组绝对门槛在写字、键盘、握力计和开合跳中产生相反含义。
_Avoid_: 固定阈值堆叠, 样本特化参数, 只按绝对 BPM 裁决

**候选资格过滤**:
运动段低锁上跳重捕获中用于排除明显不安全上跳目标的第一层判断。它关注候选是否只是单窗冒尖、是否明显贴近运动伪峰或谐波、是否会让当前合理心率区域失去可达性，以及是否与窗口序列证据严重冲突。
_Avoid_: 综合分直接裁决, 最高峰优先, 只看候选 BPM

**候选相对评分**:
在候选资格过滤之后，对合格上跳候选进行排序的证据聚合过程。它比较候选的跨窗口稳定性、幅值竞争、与历史轨迹的相对关系、与 HF/ACC 参考信号的冲突程度，以及触发后搜索范围是否仍保持合理可达。
_Avoid_: 黑盒总分, 单一幅值排序, 样本特化权重

**可达性修复**:
机制触发后优先调整搜索范围、追踪中心或候选可采纳性，使合理候选峰重新进入算法可达区域，而不是直接把最终心率改写为目标候选。它用于把运动段低锁上跳重捕获从输出补丁转为追踪状态恢复机制。
_Avoid_: 直接改写 Final HR, 单步硬切, 候选峰强制采用

**ACC 对比参考信号**:
用于在下游心率估计任务中对照热式界面传感器 HF 信号去除运动伪影能力的运动参考信号。它复用 HF 主链路确定的评估口径，不单独以提升 ACC 性能为目标进行参数优化。
_Avoid_: HF 链路触发证据, ACC 独立优化对象, ACC 辅助门控

**HF 主链路**:
以双侧热式界面传感器信号为运动伪影参考、承担本项目算法优化与跨个体泛化验证的主要心率估计路线。
_Avoid_: 多参考信号等权主线, HF/ACC 融合路线

**双侧 HF 联合参考**:
将 PPG 两侧的热式界面传感器视为共同描述局部界面动态的多通道参考，而不是彼此独立或只保留相关性最高的一路。
_Avoid_: 单侧 HF 代表, 两路独立主线, 最高相关通道替代

**正则化多输入 NLMS**:
联合使用双侧 HF 参考、按参考能量归一化更新并约束相关通道权重稳定性的线性自适应消噪主方案。
_Avoid_: 相关性排序串行 LMS, 单路硬选择, 默认非线性滤波

**HF 可解释性驱动的双侧分频消伪**:
按频带评估 PPG 成分能否被双侧 HF 解释，并依据左右通道质量、共线程度和 HF—PPG 映射稳定性约束各通道的消除贡献；只有得到因果 HF 证据支持的竞争成分才允许较强消除。参考心率只用于离线验收，不得用于运行时选择或保护频带。
_Avoid_: 双侧强制等权输入, 全频带统一强度消除, 参考 HR 频带保护, 复杂 VSS/W2 状态机

**可在线化核心**:
HF 自适应消噪与心率追踪的运行时核心只依赖当前窗口及过去状态，不使用未来窗口或参考心率；离线研究路径可保留零相位预处理，但不能据此声称端到端实时部署。
_Avoid_: 已嵌入式部署, 端到端在线系统, 使用未来信息的运行时核心

**离线训练 BO**:
只使用训练样本及其参考心率搜索可跨样本复用的共享参数，并在测试或运行阶段固定应用的参数选择过程。
_Avoid_: 在线运行时 BO, 测试样本调参, 单样本拟合上限

**测试样本 BO**:
使用被评估样本自身的参考心率选择参数的样本内优化，只表示该样本的拟合上限，不能作为跨样本或跨个体泛化证据。
_Avoid_: 跨人泛化结果, 部署参数验证, 无监督参数自适应

**旧 Lite 场景三折基线**:
旧 Lite 机制在场景内三折评估中，仅使用同场景另外两条记录及其参考心率选择共享参数，再在留出记录上固定运行的公平基线。它与新机制使用相同的数据隔离规则，用于判断机制和参数学习规则是否产生可迁移收益。
_Avoid_: 旧 Lite 单记录 BO, 历史最优曲线直接比较, 测试记录调参

**旧 Lite 单记录 BO 拟合上限**:
旧 Lite 使用每条被测记录自身参考心率独立 BO 得到的样本内能力上限。它必须与公平三折基线同时报告，但只用于衡量新方案距离当前样本内最佳工程能力的差距，不能作为基础机制成功的唯一门槛。
_Avoid_: 旧 Lite 泛化基线, 可部署旧 Lite, 公平冻结对照

**基础机制成功**:
新 HF 消伪影机制通过频谱证据门槛和下游任务门槛，并在场景内三折评估中稳定优于旧 Lite 场景三折基线。
_Avoid_: 只在开发记录改善, 只凭平均 MAE, 击败单记录 BO 才算成功

**样本内能力上限突破**:
通过基础机制成功的新方案进一步达到或优于旧 Lite 单记录 BO 拟合上限的强结果。它表示在当前记录上超过历史 Lite 的样本内调参能力，不自动构成跨场景或跨个体泛化证据。
_Avoid_: 基础成功门槛, 跨人优越性, 无答案在线算法

**下游任务基线**:
在相同心率估计任务和评价口径下，用于显现 HF 主链路去除运动伪影有效性的 ACC 或纯 FFT 对照路线，而不是需要分别优化到最优性能的并列主算法。
_Avoid_: 竞争主算法, 独立调参基线, 传感器本体指标

**低锁上跳三层验收**:
评估运动段低锁上跳重捕获优化时的优先级口径：先确认写字、键盘、握力计等防误触发场景不再大量制造不可达窗口，再确认开合跳、波比等历史救援场景仍能摆脱低锁，最后才比较总体 MAE、hit rate 和触发统计。
_Avoid_: 只看总体 MAE, 完全禁用机制的假胜利, 单一救援样本过拟合

**失败快速退出**:
运动段低锁上跳重捕获触发后，若候选连续失稳、上跳后候选不再合格、搜索范围持续远离合理区域，或输出被推向疑似谐波/运动伪峰区，机制应快速退出并回到普通追踪。它承认在线证据可能误判，防止错误状态污染后续窗口。
_Avoid_: 长时间保持错误重捕获态, 无撤销硬切, 过长冷却导致漏救援

**整段求解运动段评估**:
算法在完整样本时间线上运行，以保留运动前历史、运动段状态和阶段切换连续性，但实验指标只统计运动段自适应链路窗口。它用于避免 motion-only 裁剪求解改变进入运动段时的追踪状态。
_Avoid_: motion-only 求解, 全段指标泛称

**运动感知纯 FFT 基线**:
只使用 PPG 原始频谱及其历史轨迹估计心率、但允许感知运动段和运动后阶段边界来管理追踪状态的纯 FFT 对照路线。它不得使用自适应滤波输出、运动参考去伪影结果、参考心率或答案派生信号作为目标或门控证据。
_Avoid_: continuous FFT 泛称, 自适应辅助 FFT, 重捕获专用 FFT

**运动后保护窗**:
运动段结束后的一段过渡时间，算法仍需防止从运动链路直接切回静息链路造成不合理跳变。保护窗长度是需要通过实验评估的设计变量。
_Avoid_: 恢复段, recovery

**运动后动态保护窗**:
从运动结束到 Final 输出切回 reset FFT 链路之间的样本级过渡阶段。它不是固定秒数，而是由稳定交汇或持续高差回切等证据结束。
_Avoid_: 固定恢复窗, 动态运动保护窗口

**稳定重捕获保护窗长**:
运动结束后等待 PPG 波形大概率摆脱运动伪影主导、足以支持纯 FFT 重新捕获心率的全局默认保护窗长度。它用于评选运动感知纯 FFT 基线，不表示按样本动态调节的门控延迟。
_Avoid_: 最优切换延迟, 样本自适应保护窗, recovery 固定时长

**运动后静息 FFT 重捕获**:
运动后保护窗结束后的静息状态，此时 PPG 波形若已足够干净，应允许 FFT 主频重新捕获心率，并使用适合该阶段的心率变化限幅策略。
_Avoid_: 运动后 rest, 后恢复段, 静息恢复段

**reset FFT 链路**:
运动结束后清空或弱化既有追踪历史、重新从 PPG 频谱候选中捕获心率的链路族，包括独立 reset FFT 与交接 reset FFT。两者共享 raw PPG 频谱证据，但具有不同的初始化信息边界。
_Avoid_: 普通 FFT 曲线, 静息 FFT 泛称, 单一 reset 状态

**独立 reset FFT**:
只使用 raw PPG 频谱及自身 reset 后状态重新捕获心率的纯 PPG 对照链路，不读取自适应链路或 Final 的历史。它是主报告中 FFT 对照曲线的权威来源，不参与 Final 的切换决策。
_Avoid_: 交接 reset FFT, continuous FFT, Final 辅助 FFT

**交接 reset FFT**:
使用与独立 reset FFT 相同的 raw PPG 频谱证据，但在启动阶段允许将切换前 Final 及其因果下降趋势作为弱先验的重捕获链路。它只产生可供统一切换器评估的 reset 目标，不直接改写 Final。
_Avoid_: 独立 reset FFT, 自适应滤波输出, Final 直接改写

**交接候选稳定性证据**:
交接 reset 的 selected raw 候选依据峰可见性、幅值竞争、跨窗稳定性和轨迹身份连续性形成的诊断事实。它只回答候选峰是否可信，不单独形成切换控制状态。
_Avoid_: candidate qualification 控制器, gap 即可信, 参考心率门控

**交接 tracker 收敛证据**:
交接 tracker 的公开输出已经追到稳定 raw 候选附近且未依赖 held previous 的诊断事实。它只回答执行轨迹是否追上目标，不单独授予 Final 写权限。
_Avoid_: tracker ready 控制器, selected 峰已正确, 输出自然可信

**交接目标可消费**:
交接候选稳定性证据与交接 tracker 收敛证据同时成立时形成的唯一切换许可。统一切换器只消费该许可；两个诊断事实不再各自维护 Final 控制状态。
_Avoid_: candidate qualified, switch target ready, bootstrap admissible, gap 即许可

**一次性 PPG 启动门**:
完整心率窗口全部位于运动结束后，且连续 raw PPG 序列重新表现出可用心搏周期性与谱峰连续性时打开的单向启动边界。打开后不再维护丢失、恢复或固定超时状态。
_Avoid_: 持续可观测状态机, 固定等待结束, 参考心率质量门控, reset ready

**交接 reset 一次性恢复初始化**:
一次性 PPG 启动门打开后，每个运动后段最多一次清除交接 tracker 在初期低可观测窗口形成的低锁历史，并使用当前 raw top-k 与因果 Final 弱先验重新启动。独立 reset FFT 不参与该初始化。
_Avoid_: 独立 reset 重启, 反复 reset, Final 直接改写, 恢复后再次初始化

**交接 reset 受控重锚**:
交接 reset 候选取得充分且连续的无参考 PPG 证据后，将 handoff tracker 的内部追踪状态迁移到该候选轨迹附近，以解除旧低锁状态和方向限速造成的长期不可达。重锚不直接改写 Final，也不等同于取得切换目标就绪。
_Avoid_: Final 硬切, 参考心率校正, 单窗 top-1 强制采用, 输出补丁

**运动后 Final 单写入权**:
运动后只有统一切换器可以改变 Final 的来源；独立 reset、交接 tracker、legacy dynamic guard 和诊断探针只能提供证据或目标。任何并列 Final 消费入口都违反该边界。
_Avoid_: 双控制器, dynamic guard 直接消费, tracker 直接写 Final

**运动后自然不切换**:
一次性 PPG 启动门或交接目标可消费始终未成立时，Final 自然保持 adaptive 的结果。它不是限时弃权状态，也不阻止较晚出现的合法交接。
_Avoid_: permanent safe abstain, confirmation timeout, no-switch 失败泛称

**运动后不可逆交接**:
统一切换器一旦采用交接目标，本次运动后阶段不再返回旧 adaptive Final；偶发坏窗口沿用 tracker 的保持值语义，不产生新的回退状态。
_Avoid_: post-switch hold 开关, ready 撤销后回跳, 重复交接

**运动后绝对应用门槛**:
用于判定失效样本是否真正被交接 reset 救回的绝对性能要求；目标层和切换后 Final 的固定 60 s MAE 应不高于 3 BPM，不能以相对改善率替代。E20 继续逐窗审计，但不单独否决生理连续且总体 MAE 达标的结果。
_Avoid_: 改善 50% 即可用, D1 均值门槛, 正常样本统一 3 BPM 门槛

**连续纯 FFT 诊断链路**:
只使用 raw PPG 频谱并保持自身全程追踪历史、不在运动后重新初始化的离线诊断对照。它不作为主报告的默认 FFT 曲线，也不进入 Final。
_Avoid_: 独立 reset FFT, 交接 reset FFT, 默认纯 FFT 基线

**自适应链路**:
使用 HF、ACC 或其他参考信号经自适应滤波后得到的心率估计链路。运动后动态保护窗内，Final 输出默认先沿用该链路，直到有足够证据切回交接 reset FFT。
_Avoid_: adaptive 泛称, LMS 曲线泛称

**旧 Lite BO 对照**:
历史 Lite 预设批处理输出中保存的每样本 BO 最优参数、最终心率曲线和报告文件。它是评估新运动后机制时的同源基线，不应只被当作一个聚合均值。
_Avoid_: Lite 均值基线, 固定 Lite source

**同源替换实验**:
在运动段和运动后保护窗的源曲线口径保持一致时，只替换运动后保护窗结束之后的重捕获后段，以区分源参数差异和重捕获机制本身的效果。
_Avoid_: 固定 source 对比, 混合口径 delta

**重捕获后段**:
从运动后保护窗结束后开始、到样本结束或下一段运动开始前的算法输出阶段。它是 reset FFT、首窗峰选择、边界平滑和短期 fallback 的评估对象。
_Avoid_: 整个运动后段, source 段

**首窗峰共识**:
运动后静息 FFT 重捕获开始后，使用连续多个窗口的候选谱峰稳定性来确认首个可接受心率轨迹，而不是只信任单个窗口的最大谱峰。
_Avoid_: 单窗主峰, raw peak 直接采用

**运动后追踪策略族**:
专门用于运动后静息 FFT 重捕获的心率追踪与限幅策略集合。它独立于普通静息段策略，具体参数应由实验或预设选择，并应支持上升保守、下降放宽的非对称限幅。
_Avoid_: 复用普通 rest 参数

**稳定交汇**:
交接目标可消费后，交接 reset 与当前实际 Final 在连续窗口内足够接近时，由统一切换器执行的正常入口。内部 adaptive 轨迹只能提供辅助诊断，稳定交汇不得产生硬切。
_Avoid_: 单点相交, 只看 abs gap, 内部 adaptive 交汇, 稳定交汇硬切

**可达过渡**:
从当前 Final 心率切到目标链路时，跳变量仍落在该阶段允许的上升或下降步长内。它用于区分生理可信的回切和会造成曲线断裂的硬切。
_Avoid_: 只看边界差, 任意平滑

**持续高差回切**:
交接目标可消费后，运动后 adaptive Final 与交接 reset 仍显著分离时，由统一切换器硬切到交接目标，以快速救援运动段高漂移。高差只触发执行方式，不能代替目标许可。
_Avoid_: adaptive_rising_rescue, gap-only rescue

**取得资格后的快速硬切**:
交接目标可消费且持续高差回切成立时，统一切换器在当前窗口立即采用交接目标的救援执行方式。它保留快速纠正高漂移的能力，但禁止 gap-only 硬切。
_Avoid_: 无条件硬切, gap-only hard switch, 普通稳定交汇

**运动后静息段指标**:
只统计运动后保护窗结束后、到样本结束或下一段运动开始前的心率估计误差与命中率。它是评估运动后静息 FFT 重捕获是否成功的主指标。
_Avoid_: 只看 total/rest/motion 汇总

**运动后分池防退化**:
失效样本池与正常样本池分别汇总运动后误差；正常池平均 MAE 回归受限，单条明显回归必须解释，失效池收益不得抵消正常池整体退化。E20 是审计项，非生理反向跳变是连续性硬失败。
_Avoid_: 全 24 条单一均值, 失败收益抵消正常退化, E20 单独一票否决

**固定 60 s post-motion MAE**:
从运动结束点开始固定取后续 60 秒，计算 Final 心率与参考心率的平均绝对误差。它用于观察恢复早期，避免长静息尾段稀释运动刚结束后的失败。
_Avoid_: 完整 post-motion MAE, 切换后 MAE

**交接重捕获初始化**:
交接 reset FFT 启动时，将切换前 Final 及其因果下降趋势作为候选排序与可达性判断的弱先验，同时要求输出仍由 raw PPG 候选证据产生。
_Avoid_: 直接继承保护窗末端 HR, Final 直接改写, 强历史约束

**静息段**:
未处于运动段，也不属于运动后保护窗的低运动状态。运动前静息与运动后静息可能需要不同的追踪和限幅策略。
_Avoid_: rest 泛称

**窗口阶段**:
报告和诊断中描述窗口所属生理与算法阶段的分类，例如运动前静息、运动段、运动后保护窗和运动后静息 FFT 重捕获。它比旧的 rest/motion/recovery 分类更精确。
_Avoid_: window_kind 旧三分类

**机制级路线审计**:
以一次算法机制变更为最小审计单元，串联动机、实现、实验输出、窗口诊断、泛化收益、退化风险和去留建议。
_Avoid_: 只看提交列表, 只看最终 AAE

**外部泛化样本**:
cross_person 评估中不参与共享 BO 参数训练、只用于复核跨个体泛化表现的被试外样本。
_Avoid_: 测试集泛称, own_train

**冻结压力评估集**:
失效现象已经被观察、但在机制和阈值冻结前不参与设计、筛选或调参，只在冻结后影响 GO/NO-GO 的记录集合。它可验证已知失效是否跨个体改善，但不能被表述为完全未见泛化集。
_Avoid_: 开发集, 调参集, 完全未见测试集, 只展示样本

**v2 默认运动后回切机制**:
v2 心率算法在整段分析且存在运动段时默认采用的运动后链路切换语义：Final 先沿用自适应链路，并在运动后动态保护窗中根据稳定交汇或持续高差回切等证据切回 reset FFT 链路。旧固定保护窗回切只作为显式兼容路径。
_Avoid_: Lite 特例开关, GUI 隐式修复, 固定 20s 默认回切

**权重状态策略**:
描述自适应滤波器权重在相邻心率窗口之间如何管理的实验因素。至少区分独立窗口权重与连续流权重；跨窗口方案必须只处理新到达的样本一次，不能因窗口重叠而重复训练旧样本。
_Avoid_: 跨窗状态, 是否继承权重, 默认连续权重

**独立窗口权重**:
每个心率窗口开始时将自适应滤波权重重置，再仅使用该窗口内的数据完成适应与输出；用于隔离单窗口内参考信号的去伪影能力。
_Avoid_: 每窗清零, 冷启动窗口, reset 模式

**连续流权重**:
首个窗口完成初始化后，在后续窗口之间保留自适应滤波权重，并且每次窗口推进只用新到达的非重叠数据更新权重；用于模拟真实在线数据流中的持续学习。
_Avoid_: 跨窗继承, warm start, 状态保留模式

**固定 HF 前视**:
所有 NLMS 阶数条件共享的最大正向时间对齐范围，本轮固定为 200 ms；物理记忆长度只表示向历史方向的覆盖，使对齐能力不随阶数一起变化。
_Avoid_: 非因果时延范围, 阶数自带延迟, 双向记忆长度

**共享因果 HF 归一化**:
双侧 HF 各自使用按时间顺序持续更新的幅值归一化状态，并让独立窗口与连续流权重实验共享同一归一化输入；每个原始样本只参与一次统计量更新。
_Avoid_: 每窗 z-score, 权重模式专属归一化, 重叠窗口重复归一化

**相对 NLMS 正则化**:
按双侧 HF 输入向量维数缩放 NLMS 分母保护项的定义，使不同物理记忆长度下的正则化相对强度保持可比。
_Avoid_: 固定绝对 epsilon, 阶数相关分母保护, 正则化常数泛称

**权重半衰期**:
描述泄漏 NLMS 中既有权重衰减到一半所需的物理时间，用于跨采样率比较旧运动模式的遗忘速度；无穷大表示不泄漏。
_Avoid_: 逐点泄漏系数, lambda 泛称, 权重衰减率

**共差模 HF 表示**:
将归一化后的双侧 HF 正交变换为共同变化分量与左右差异分量的参考表示，用于分析对称结构中的共同运动和局部形变；它与原始双侧表示包含相同线性信息。
_Avoid_: 新增两路 HF, raw 加 sum/difference, 数据驱动翻转

**三维频谱证据**:
分别从 HF 支持的伪影抑制、真实心率峰暴露和脉搏频谱保留三个维度验收自适应滤波结果，不预先压缩为单一加权分数。
_Avoid_: 频谱干净度总分, 单指标频谱收益, 只看峰值排名

**参考心率邻域**:
围绕窗口参考心率定义的频谱保护与评价范围，主分析采用正负 10 BPM，并以正负 7.5 和 15 BPM 检查带宽敏感性。
_Avoid_: 单个参考频点, FFT 补零分辨率, 任意心率保护带

**双侧 HF 频谱软并集**:
由两路归一化 HF 功率谱在每个频率上的较强响应构成的连续伪影支持权重；任意一侧捕捉到的局部运动都能进入评价，但不需要硬选伪影峰。
_Avoid_: HF 峰值清单, 双侧谱简单拼接, 硬阈值伪影频带

**记录级统计单位**:
将一条完整采集记录视为本轮扩大实验的最小独立单位；重叠窗口只用于记录内机制诊断和汇总，不作为彼此独立的统计重复。
_Avoid_: 窗口级样本量, 重叠窗口独立检验, CGX 跨人统计

**双哨兵非对称准入**:
候选必须在困难哨兵上产生机制与心率收益，同时将简单哨兵作为硬性防退化约束；不得用两条记录的平均分掩盖任一侧失败。
_Avoid_: 双样本平均排名, 困难样本单独最优, 简单样本补偿项

**E10/E20 尾部风险**:
分别统计心率绝对误差超过 10 BPM 和 20 BPM 的窗口，用于定位严重失锁及其发生阶段。它们是 MAE 的逐窗审计，不单独等同于非生理切换或自动 NO-GO。
_Avoid_: 大误差泛称, E20 一票否决, 尾部总分

**运动后反向跳变**:
运动后 Final 单窗下降至少 20 BPM，并在随后 5 个窗口内相对低点回升至少 20 BPM 的非生理往返事件。合法的一次性硬切不属于反向跳变；任何候选出现该事件都属于连续性硬失败。
_Avoid_: 最大单窗跳变, E20 窗口, 正常恢复波动

**双口径权重状态比较**:
同时报告 W0/W1 在相同滤波参数下的权重状态配对效应，以及两种状态分别从稳定参数平台配置后达到的最佳可达性能；前者是主机制证据。
_Avoid_: 只比各自最优, 强制共享最终参数, 权重继承总收益

**稳定参数平台**:
在记忆长度与步长响应面上连续通过机制和防退化门槛的二维区域，最低要求包含一个完整的相邻二乘二网格；孤立点或单轴细线不属于平台。
_Avoid_: 单点最优, 若干优秀参数, 热图高值区

**规则化网格细化**:
粗网格完成后按预先声明的内部加密、边界扩展或失败诊断规则执行的唯一一次补充搜索；它不得演变为围绕哨兵样本反复追逐最优点。
_Avoid_: 迭代调参, 人工扩网格, 隐性 BO

**稳定性全因子**:
在稳定参数平台代表上联合考察相对正则化、权重半衰期和双侧 HF 通道表示的第二阶段实验，用于识别这些稳定化机制的主效应与交互效应。
_Avoid_: 单因素稳定性调参, 第二阶段 BO, 稳定机制逐项最优

**W2 因果证据边界**:
门控连续权重只能依据当前及历史的 PPG、双侧 HF、残差、权重状态和数据质量采取动作；人工运动标签与参考心率仅用于离线评价。
_Avoid_: 标签驱动重置, 参考心率门控, 回看式状态切换

**W2 分级动作**:
门控连续权重按证据强度依次采用正常更新、暂时冻结、泄漏遗忘和完全重置，分别处理有效学习、短暂无证据、映射持续变化和旧状态失效。
_Avoid_: 单阈值清零, 冻结重置泛称, 频繁硬切权重

**HF 证据驱动 VSS**:
由双侧 HF 活动、伪影可解释性和更新一致性等因果证据生成有界 NLMS 步长的项目定制变步长机制；它不使用参考心率或人工运动标签。
_Avoid_: BO 步长, 参考心率驱动步长, 黑箱 VSS

**V4 三证据**:
HF 证据驱动 VSS 中分别表示双侧 HF 活动度、残余可解释性和梯度一致性的三个有界分量，并通过 A、AR、ARG 嵌套消融验证各自贡献。
_Avoid_: V4 综合质量分, 可训练证据权重, 不可拆解步长特征

**VSS 步长包络**:
以稳定固定步长为中心定义的有界相对调制范围，用于比较保守、平衡和宽范围的在线步长变化，而不重新搜索样本专属绝对步长。
_Avoid_: VSS 上下界 BO, 独立绝对步长范围, 无界变步长

**V4 共享证据时间尺度**:
HF 活动度、残余可解释性和梯度一致性共同使用的物理平滑时长，用于限制 V4 自由度并在不同采样率下保持相同响应速度。
_Avoid_: 每证据独立时间常数, 步长额外平滑, 采样点平滑长度

**原则冻结与数值候选**:
实验架构、因果信息边界、阶段顺序和验收逻辑作为稳定原则；具体网格、阈值与时间常数只是首轮候选，可依据响应面和失败证据版本化修订。
_Avoid_: 候选值即最终参数, 结果后随意调参, 每样本专属修订

**首次冻结扩大验证**:
在查看 CGX 24 样本整体结果前冻结主方案与备选方案并统一运行的首次扩大检查；若其结果被用于修改算法，后续轮次必须标记为开发反馈而非未见验证。
_Avoid_: CGX 边跑边调, 扩大集独立验证泛称, 失败样本专调

**最终证据矩阵**:
由纯 PPG、冻结 ACC、单侧 HF、双侧固定步长、双侧变步长和双侧完整状态方案构成的对照集合，分别回答参考信号、双侧结构和算法机制的增量价值。
_Avoid_: 算法排行榜, ACC 独立优化矩阵, 只比最终方案

**增量复杂度保留**:
只有同时产生稳定频谱证据、下游心率增量并通过防退化门槛的附加机制才进入最终算法；没有稳定增量时回退到更简单候选。
_Avoid_: 为创新强留机制, 最复杂方案默认胜出, 只按平均 AAE 保留
