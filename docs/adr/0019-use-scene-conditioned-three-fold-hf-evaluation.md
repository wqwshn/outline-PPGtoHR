---
status: accepted
---

# 使用场景条件化三折评估 HF 机制

CGX24 只有一个个体、8 个场景且每场景 3 条记录，同时已经参与上一轮开发反馈。新的 HF 消伪研究允许同一机制按已知场景使用固定参数，但必须对每个场景执行两条训练、一条留出的三折评估，并另跑全场景统一参数口径；这在有限数据下优先检验参数学习规则的场景内稳定性，同时明确放弃“未见数据”与“无场景标签通用算法”的主张。旧 Lite 使用相同三折隔离作为公平基线，测试记录自身 BO 只作为拟合上限；24 条相关记录不得按独立重复做胜数显著性判断，稳定性以场景分层效应和成簇失败为主要依据。
