# 将四场景开发范围中的 xiezi 替换为 quanji

四场景共享参数开发闭环改用 `jianpan`、`kaihe`、`run` 与 `quanji`；`xiezi` 转为 HF 参考适用性待审场景，因为其 HF 相对 FFT 的收益较小且记录间方向不一致，同时共享参数研究持续受到单记录失锁阻塞。`quanji` 从旧资格任务的确认防火墙转入开发范围，这一角色消费使其不再具备该路径的局部确认独立性；`bobi`、`tiaosheng` 与 `woli` 继续留在防火墙后，且本决定不把 xiezi 归因为采集故障。
