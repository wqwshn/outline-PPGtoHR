# 在 CGX 24 首次扩大运行前冻结候选

从机制阶段选出一个主方案和最多两个备选后，首次运行 CGX 24 前冻结代码、参数与判定逻辑，并以统一配置处理全部记录；主结果不允许逐记录 BO 或人工调参，测试样本 BO 只作为拟合上限另行报告。若首次结果暴露系统性失败，可以版本化返回机制阶段统一修改并重新完整运行，但从 CGX 24 反馈产生的后续轮次必须标记为开发迭代，不能再宣称是未见验证，以保留首次扩大检查的解释边界。
